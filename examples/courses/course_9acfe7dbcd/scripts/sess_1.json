{
  "session_id": "sess_1",
  "course_id": "course_9acfe7dbcd",
  "title": "为什么要用多项式逼近函数？",
  "learning_goal": "理解为什么需要多项式逼近，并认识到逼近是局部的。",
  "steps": [
    {
      "title": "手算 sin(0.5) 的挑战",
      "beat": "hook",
      "spoken_text": "先别翻公式。看第一行：sin(0.5) 等于多少？你手边没有计算器，能算吗？sin x 和 e 的 x 次方这种函数，我们只会按计算器，不会手算。那问题来了——有没有一种东西，我们闭着眼睛也能算，还能替它们干活？",
      "boards": [
        {
          "title": "",
          "markdown": "- $\\sin(0.5) = ?$ 手算不了\n- $e^{0.5} = ?$ 手算不了\n- 计算器一按就有 → 它凭什么？",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "计算器一按就有",
          "board_index": 0,
          "trigger_phrase": null,
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "没有计算器，你怎么知道 sin(0.5) 大约是多少？",
        "options": [
          "查表或者背过常用值",
          "用加法和乘法硬算出来",
          "只能按计算器，没别的办法"
        ],
        "correct_index": 1,
        "misconceptions": [
          "查表确实能查到，但表也是别人算好的，你还是不会自己算。",
          null,
          "计算器内部其实就是用加法和乘法算的，只是你不知道而已。"
        ],
        "explanation": "计算器内部用的正是多项式——只靠加法和乘法，就能把 sin(0.5) 算到很高精度。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "多项式是万能积木",
      "beat": "analogy",
      "spoken_text": "看第二块板书。多项式长什么样？a0 加 a1 乘 x 加 a2 乘 x 平方，一直加下去。它只干两件事：加法和乘法。就像乐高积木，只有凸点和凹槽，却能拼出城堡。而 sin x、e 的 x 次方像一座复杂的雕塑，好看但难复制。我们能不能用乐高积木拼出这座雕塑的局部？",
      "boards": [
        {
          "title": "多项式：只有加法和乘法",
          "markdown": "- $p(x) = a_0 + a_1 x + a_2 x^2 + \\cdots$\n- 只用到 **加法** 和 **乘法**\n- 像乐高积木 → 简单、可拼\n- $\\sin x$、$e^x$ 像雕塑 → 复杂、难手算",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "highlight",
          "snippet": "加法",
          "board_index": 0,
          "trigger_phrase": "只干两件事",
          "color": null
        },
        {
          "kind": "highlight",
          "snippet": "乘法",
          "board_index": 0,
          "trigger_phrase": "加法和乘法",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "为什么偏偏选多项式来当积木？",
        "options": [
          "因为多项式可以无限逼近任何函数",
          "因为多项式次数高，什么都能表示",
          "因为多项式只用到加法和乘法，最好算"
        ],
        "correct_index": 2,
        "misconceptions": [
          "能逼近是结果，不是原因——选它是因为它好算。",
          "次数高不等于好算，而且高次多项式反而更难控制。",
          null
        ],
        "explanation": "多项式只涉及加法和乘法，手算和计算机都最容易处理，这才是选它的理由。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "什么叫“长得一模一样”",
      "beat": "define",
      "spoken_text": "现在把目标说清楚。看板书最后一行：给定一个光滑函数 f 和一个点 a，我们要找一个多项式 p，让它在 a 附近和 f 贴得很近。注意关键词是“附近”，不是“处处”。这个附近到底能有多近？先记住这个问号，下一步我们动手量一量。",
      "boards": [
        {
          "title": "逼近的目标",
          "markdown": "- 给定：光滑函数 $f$，点 $a$\n- 找一个多项式 $p$\n- 要求：在 $a$ **附近** $|p(x)-f(x)|$ 很小\n- 注意：是 **附近**，不是处处",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "附近",
          "board_index": 0,
          "trigger_phrase": "关键词是“附近”",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "试试 p(x)=x 逼近 sin x",
      "beat": "worked_example",
      "spoken_text": "看右边这张数值表，三行数字。第一列是 x，第二列是 sin x，第三列是 p 等于 x，第四列是误差。x 等于 0.1 时，sin 是 0.0998，p 是 0.1，误差只有 0.0002。x 等于 0.5 时误差涨到 0.0206。x 等于 1.0 时误差 0.1585。再看表下面这张图，蓝线是 sin x，红线是 y 等于 x，两条线在原点附近几乎贴在一起，往右走就分开了。我把探针推到 1.2，你看读数——误差已经很大了。那这个多项式在 0.5 附近还算准吗？",
      "boards": [
        {
          "title": "p(x)=x 逼近 sin x",
          "markdown": "| x | sin x | p=x | 误差 |\n|---|---|---|---|\n| 0.1 | 0.0998 | 0.1 | 0.0002 |\n| 0.5 | 0.4794 | 0.5 | 0.0206 |\n| 1.0 | 0.8415 | 1.0 | 0.1585 |\n\n- 原点附近几乎重合\n- 越远 → 误差越大",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "highlight",
          "snippet": "0.0206",
          "board_index": 0,
          "trigger_phrase": "x 等于 0.5 时误差涨到 0.0206",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "把探针从 0 慢慢推到 1.0，误差怎么变？",
        "options": [
          "误差一直很小，到某个点突然崩掉",
          "误差先慢后快，但不会突然崩掉",
          "误差始终为零，因为 p 就是 sin 的近似"
        ],
        "correct_index": 1,
        "misconceptions": [
          "误差是连续增大的，不会突然跳变。",
          null,
          "p(x)=x 只在原点附近近似，远处误差很大。"
        ],
        "explanation": "误差随 x 增大而单调增大，从 0.0002 到 0.1585，是渐变不是突变。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "局部 vs 全局",
      "beat": "contrast",
      "spoken_text": "看这张对比表，两列。左边是全局逼近：要求多项式在所有地方都像原函数，远处误差变大，做不到。右边是局部逼近：只要求在 a 点附近误差很小，远处不管。看右边这一列的第二行，a 点附近误差可以很小，这是可以接受的。所以泰勒展开的目标不是全局，而是局部。那这个附近到底能有多近？",
      "boards": [
        {
          "title": "全局 vs 局部",
          "markdown": "| | 全局逼近 | 局部逼近 |\n|---|---|---|\n| 要求 | 处处都像 | 只在 $a$ 附近像 |\n| 远处 | 误差变大 | 不管 |\n| 可行性 | 做不到 | **可以做到** |",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "**可以做到**",
          "board_index": 0,
          "trigger_phrase": "这是可以接受的",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "动机已明，方法待寻",
      "beat": "recap",
      "spoken_text": "看最后这块板书，三行。第一行：多项式运算简单，只有加法和乘法。第二行：我们希望在一点附近用多项式近似光滑函数。第三行：逼近是局部的，不是全局。回到开头那个问题——计算器凭什么能算 sin(0.5)？现在你知道，它用的就是局部多项式。但系数怎么找？这就是下一节要解决的。",
      "boards": [
        {
          "title": "本节要点",
          "markdown": "- 多项式：只有 **加法** 和 **乘法**\n- 目标：在一点附近用多项式近似光滑函数\n- 逼近是 **局部的**，不是全局的\n- 下一步 → 怎么找系数？",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "spotlight",
          "snippet": "怎么找系数？",
          "board_index": 0,
          "trigger_phrase": "系数怎么找",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": {
        "title": "局部逼近",
        "description": "多项式简单，光滑函数复杂；用多项式在一点附近近似光滑函数，逼近是局部的，不是全局的。"
      },
      "new_page_title": null
    }
  ],
  "exercises": [
    {
      "exercise_id": "sess_1_ex1",
      "kind": "fill_blank",
      "stem": "多项式 $p(x)=a_0+a_1x+a_2x^2+\\cdots$ 之所以容易计算，是因为它只用到加法和 ____ 两种运算。",
      "options": [],
      "correct_index": null,
      "answer": "乘法",
      "accepted": [
        "乘",
        "相乘",
        "乘积"
      ],
      "explanation": "多项式只包含加法与乘法两种基本运算，因此可以手算或由计算机快速求值，这正是它作为逼近工具的优势。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_1_ex2",
      "kind": "single_choice",
      "stem": "用 $p(x)=x$ 逼近 $\\sin x$，当 $x$ 从 $0.1$ 增大到 $1.0$ 时，误差从 $0.0002$ 增大到 $0.1585$。这说明了多项式逼近的什么特点？",
      "options": [
        "逼近是局部的，离展开点越远误差越大",
        "逼近是全局的，在所有点误差都相同",
        "逼近误差与 $x$ 的大小无关",
        "多项式 $p(x)=x$ 在所有点都精确等于 $\\sin x$"
      ],
      "correct_index": 0,
      "answer": null,
      "accepted": [],
      "explanation": "从数值表可见，$x$ 离原点越远，$|p(x)-\\sin x|$ 越大，说明这种逼近只在原点附近有效，是局部逼近。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_1_ex3",
      "kind": "single_choice",
      "stem": "已知光滑函数 $f$ 在 $a=0$ 附近可用多项式 $p(x)=x$ 近似，且当 $|x|\\le 0.1$ 时误差不超过 $0.0002$。若要求误差不超过 $0.001$，则 $x$ 的取值范围大致是？",
      "options": [
        "$|x|\\le 0.1$",
        "$|x|\\le 0.5$",
        "$|x|\\le 1.0$",
        "$|x|\\le 2.0$"
      ],
      "correct_index": 0,
      "answer": null,
      "accepted": [],
      "explanation": "由表可知，$x=0.1$ 时误差 $0.0002<0.001$；$x=0.5$ 时误差 $0.0206>0.001$。因此满足误差不超过 $0.001$ 的范围应比 $0.5$ 小，选项中只有 $|x|\\le 0.1$ 符合。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_1_ex4",
      "kind": "interactive",
      "stem": "通过调整多项式 $p(x)=x$ 的系数，观察它在原点附近逼近 $\\sin x$ 的效果。",
      "options": [
        "当 $k=1$ 时，$x=0.5$ 处误差约为 $0.0206$",
        "当 $k=1$ 时，$x=0.5$ 处误差为 $0$",
        "当 $k=0.5$ 时，$x=0.5$ 处误差最小",
        "无论 $k$ 取何值，$x=0.5$ 处误差都相同"
      ],
      "correct_index": 0,
      "answer": null,
      "accepted": [],
      "explanation": "通过调节 $k$ 可以发现，$k=1$ 时直线 $y=x$ 在原点附近与 $\\sin x$ 最贴合，此时 $x=0.5$ 处误差为 $0.0206$，与数值表一致。",
      "widget": {
        "kind": "explorable",
        "title": "用 $p(x)=kx$ 局部逼近 $\\sin x$",
        "task": "拖动滑块改变斜率 $k$，使直线 $p(x)=kx$ 在 $x=0$ 附近尽可能贴合曲线 $y=\\sin x$。观察误差 $|p(x)-\\sin x|$ 在 $x=0.5$ 处的变化。",
        "html": "<!DOCTYPE html>\n<html>\n<head><script>/**\n * HandChart — zero-dependency hand-drawn chart base for explorable widgets.\n *\n * Injected into every generated 2D explorable (widget_generator.py) so the\n * model supplies data + overlays instead of hand-writing axes. The look matches\n * the handwritten page: paper panel, slightly wobbly ink axes, main red /\n * aux green, handwriting labels.\n *\n * API (all coordinates in WORLD units unless noted):\n *   const ch = HandChart.render(canvas, opts)\n *     opts = { type: \"function\"|\"points\"|\"bars\"|\"lines\",\n *              xlim:[x0,x1], ylim:[y0,y1],                  (all but bars)\n *              fn: x=>y | fns:[{fn, color?, label?}],        (function)\n *              points:[[x,y],...],                          (points)\n *              series:[{name, points, color?, dash?}],      (lines; legend drawn)\n *              bars:{labels:[...], values:[...], highlight?} (bars)\n *              xlabel, ylabel, grid:true|false, title }\n *     returns { ctx, sx, sy, box, W, H, opts }  — keep it to draw overlays\n *   HandChart.marker(ch, x, y, {color, label, r})       dot + optional label\n *   HandChart.vline(ch, x, {color, dash, label})         vertical guide\n *   HandChart.hline(ch, y, {color, dash, label})         horizontal guide\n *   HandChart.segment(ch, [x1,y1], [x2,y2], {color, width, dash, arrow})\n *   HandChart.note(ch, x, y, text, {color, align, size}) handwriting annotation\n *   HandChart.attachProbe(canvas, {xlim, ylim}, onProbe, onLeave?)\n *     pointer follow; onProbe({x, y}) in world coords — redraw + readouts there.\n *   HandChart.style = { INK, AXIS, MAIN, AUX, GRID, PAPER, MUTED }\n */\n(function () {\n  const style = {\n    INK: \"#2b2b2b\", AXIS: \"#8a8377\", MAIN: \"#c0392b\", AUX: \"#2e8b6f\",\n    GRID: \"#ece5d4\", PAPER: \"#fbfaf6\", MUTED: \"#8a8377\", PURPLE: \"#7c3aed\", BLUE: \"#2563eb\",\n  };\n  const PALETTE = [style.MAIN, style.AUX, style.BLUE, style.PURPLE, \"#d97706\"];\n  const PAD = { l: 48, r: 18, t: 18, b: 36 };\n  const HAND = \"'Patrick Hand', 'LXGW WenKai Lite', 'PingFang SC', sans-serif\";\n\n  function font(size, italic) { return (italic ? \"italic \" : \"\") + size + \"px \" + HAND; }\n\n  // deterministic wobble so the \"hand\" does not shake on every redraw\n  function wob(seed, amp) { return Math.sin(seed * 12.9898) * amp; }\n\n  function tickVals(min, max, n) {\n    const span = max - min || 1;\n    const raw = span / n;\n    const mag = Math.pow(10, Math.floor(Math.log10(raw)));\n    const step = [1, 2, 2.5, 5, 10].map((m) => m * mag).find((s) => span / s <= n) || mag * 10;\n    const out = [];\n    for (let v = Math.ceil(min / step) * step; v <= max + step * 1e-9; v += step) out.push(+v.toFixed(10));\n    return out;\n  }\n\n  function fmt(v) {\n    const a = Math.abs(v);\n    if (a === 0) return \"0\";\n    if (a >= 10000 || a < 0.01) return v.toExponential(1);\n    return String(+v.toFixed(Math.max(0, 3 - Math.floor(Math.log10(a)))));\n  }\n\n  /** a hand-drawn line: two passes with tiny deterministic wobble */\n  function inkLine(ctx, x1, y1, x2, y2, color, width, seed) {\n    ctx.save();\n    ctx.strokeStyle = color; ctx.lineWidth = width; ctx.lineCap = \"round\";\n    const n = 6, s = seed || (x1 + y2);\n    ctx.beginPath();\n    for (let i = 0; i <= n; i++) {\n      const t = i / n, x = x1 + (x2 - x1) * t, y = y1 + (y2 - y1) * t;\n      const w = i === 0 || i === n ? 0 : wob(s + i, 0.7);\n      i ? ctx.lineTo(x + w, y + w) : ctx.moveTo(x, y);\n    }\n    ctx.stroke();\n    ctx.restore();\n  }\n\n  function frame(ctx, W, H) {\n    ctx.clearRect(0, 0, W, H);\n    ctx.save();\n    ctx.fillStyle = style.PAPER;\n    ctx.fillRect(0, 0, W, H);\n    ctx.restore();\n  }\n\n  function plotBox(W, H, opts) {\n    const t = PAD.t + (opts && opts.title ? 18 : 0);\n    return { x0: PAD.l, y0: t, x1: W - PAD.r, y1: H - PAD.b };\n  }\n\n  function mapping(box, xlim, ylim) {\n    const [x0, x1] = xlim, [y0, y1] = ylim;\n    return {\n      sx: (v) => box.x0 + (v - x0) / (x1 - x0) * (box.x1 - box.x0),\n      sy: (v) => box.y1 - (v - y0) / (y1 - y0) * (box.y1 - box.y0),\n    };\n  }\n\n  function axes(ctx, box, opts) {\n    const [x0, x1] = opts.xlim, [y0, y1] = opts.ylim;\n    const { sx, sy } = mapping(box, opts.xlim, opts.ylim);\n    const xt = tickVals(x0, x1, 6).filter((v) => v >= x0 && v <= x1);\n    const yt = tickVals(y0, y1, 4).filter((v) => v >= y0 && v <= y1);\n    if (opts.grid !== false) {\n      ctx.save(); ctx.strokeStyle = style.GRID; ctx.lineWidth = 1;\n      for (const v of xt) { ctx.beginPath(); ctx.moveTo(sx(v), box.y0); ctx.lineTo(sx(v), box.y1); ctx.stroke(); }\n      for (const v of yt) { ctx.beginPath(); ctx.moveTo(box.x0, sy(v)); ctx.lineTo(box.x1, sy(v)); ctx.stroke(); }\n      ctx.restore();\n    }\n    // axes sit on zero when zero is inside the range, else on the box edge\n    const ax = y0 <= 0 && y1 >= 0 ? sy(0) : box.y1;\n    const ay = x0 <= 0 && x1 >= 0 ? sx(0) : box.x0;\n    inkLine(ctx, box.x0 - 4, ax, box.x1 + 6, ax, style.AXIS, 1.4, 1);\n    inkLine(ctx, ay, box.y1 + 4, ay, box.y0 - 6, style.AXIS, 1.4, 2);\n    // arrow heads\n    ctx.save(); ctx.fillStyle = style.AXIS;\n    ctx.beginPath(); ctx.moveTo(box.x1 + 8, ax); ctx.lineTo(box.x1 + 1, ax - 3.5); ctx.lineTo(box.x1 + 1, ax + 3.5); ctx.fill();\n    ctx.beginPath(); ctx.moveTo(ay, box.y0 - 8); ctx.lineTo(ay - 3.5, box.y0 - 1); ctx.lineTo(ay + 3.5, box.y0 - 1); ctx.fill();\n    ctx.restore();\n    ctx.save();\n    ctx.fillStyle = style.MUTED; ctx.font = font(12);\n    ctx.textAlign = \"center\";\n    for (const v of xt) {\n      if (Math.abs(sx(v) - ay) < 1 && ay !== box.x0) continue; // skip the label under the y axis\n      inkLine(ctx, sx(v), ax - 3, sx(v), ax + 3, style.AXIS, 1, v);\n      ctx.fillText(fmt(v), sx(v), ax + 16);\n    }\n    ctx.textAlign = \"right\";\n    for (const v of yt) {\n      if (Math.abs(sy(v) - ax) < 1 && ax !== box.y1) continue;\n      inkLine(ctx, ay - 3, sy(v), ay + 3, sy(v), style.AXIS, 1, v + 7);\n      ctx.fillText(fmt(v), ay - 7, sy(v) + 4);\n    }\n    ctx.font = font(14, true); ctx.fillStyle = style.INK;\n    if (opts.xlabel) { ctx.textAlign = \"right\"; ctx.fillText(opts.xlabel, box.x1 + 4, ax - 8); }\n    if (opts.ylabel) { ctx.textAlign = \"left\"; ctx.fillText(opts.ylabel, ay + 8, box.y0 + 4); }\n    ctx.restore();\n    return { sx, sy };\n  }\n\n  function stroke(ctx, pts, color, width, dash) {\n    if (!pts.length) return;\n    ctx.save();\n    ctx.strokeStyle = color; ctx.lineWidth = width || 2.2;\n    ctx.lineJoin = \"round\"; ctx.lineCap = \"round\";\n    if (dash) ctx.setLineDash(dash);\n    ctx.beginPath();\n    let pen = false;\n    for (const p of pts) {\n      if (!p) { pen = false; continue; }\n      pen ? ctx.lineTo(p[0], p[1]) : ctx.moveTo(p[0], p[1]);\n      pen = true;\n    }\n    ctx.stroke();\n    ctx.restore();\n  }\n\n  function fnPoints(fn, opts, m) {\n    const [x0, x1] = opts.xlim, [y0, y1] = opts.ylim;\n    const n = 400, pts = [];\n    const margin = (y1 - y0) * 0.02;\n    for (let i = 0; i <= n; i++) {\n      const x = x0 + (x1 - x0) * i / n;\n      let y;\n      try { y = fn(x); } catch (e) { y = NaN; }\n      if (!isFinite(y) || y < y0 - margin || y > y1 + margin) { pts.push(null); continue; }\n      pts.push([m.sx(x), m.sy(y)]);\n    }\n    return pts;\n  }\n\n  function legend(ctx, box, items) {\n    if (!items.length) return;\n    ctx.save();\n    ctx.font = font(13); ctx.textAlign = \"left\"; ctx.textBaseline = \"middle\";\n    let x = box.x0 + 10, y = box.y0 + 12;\n    for (const it of items) {\n      inkLine(ctx, x, y, x + 22, y, it.color, 2.4, x);\n      ctx.fillStyle = style.INK;\n      ctx.fillText(it.label, x + 28, y + 1);\n      x += 28 + ctx.measureText(it.label).width + 18;\n    }\n    ctx.restore();\n  }\n\n  function title(ctx, W, text) {\n    ctx.save();\n    ctx.font = font(15); ctx.fillStyle = style.INK; ctx.textAlign = \"center\";\n    ctx.fillText(text, W / 2, 16);\n    ctx.restore();\n  }\n\n  function render(canvas, opts) {\n    const ctx = canvas.getContext(\"2d\");\n    const rect = canvas.getBoundingClientRect();\n    const W = Math.max(60, Math.round(rect.width)), H = Math.max(60, Math.round(rect.height));\n    const dpr = window.devicePixelRatio || 1;\n    if (canvas.width !== Math.round(W * dpr) || canvas.height !== Math.round(H * dpr)) {\n      canvas.width = Math.round(W * dpr); canvas.height = Math.round(H * dpr);\n    }\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n    frame(ctx, W, H);\n    if (opts.title) title(ctx, W, opts.title);\n    const box = plotBox(W, H, opts);\n    const type = opts.type || \"function\";\n\n    if (type === \"bars\") {\n      const labels = opts.bars.labels || [], values = opts.bars.values || [];\n      const n = Math.max(1, values.length);\n      const bw = (box.x1 - box.x0) / n;\n      const vmax = Math.max(...values.map((v) => Math.abs(v)), 0) * 1.15 || 1;\n      const base = box.y1;\n      inkLine(ctx, box.x0 - 4, base, box.x1 + 4, base, style.AXIS, 1.4, 3);\n      ctx.save();\n      values.forEach((v, i) => {\n        const h = (Math.abs(v) / vmax) * (box.y1 - box.y0);\n        const x = box.x0 + i * bw + bw * 0.2, w = bw * 0.6, y = base - h;\n        const hot = opts.bars.highlight != null && i === opts.bars.highlight;\n        ctx.fillStyle = hot ? style.MAIN : \"#e6dfcf\";\n        ctx.fillRect(x, y, w, h);\n        ctx.strokeStyle = hot ? style.MAIN : style.AXIS; ctx.lineWidth = 1.2;\n        ctx.strokeRect(x + 0.5, y + 0.5, w - 1, h - 1);\n        ctx.font = font(13); ctx.textAlign = \"center\"; ctx.fillStyle = style.INK;\n        ctx.fillText(String(labels[i] == null ? i + 1 : labels[i]), x + w / 2, base + 17);\n        ctx.fillStyle = hot ? style.MAIN : style.MUTED;\n        ctx.fillText(fmt(v), x + w / 2, y - 6);\n      });\n      ctx.restore();\n      const sx = (i) => box.x0 + (i + 0.5) * bw, sy = (v) => base - (Math.abs(v) / vmax) * (box.y1 - box.y0);\n      return { ctx, sx, sy, box, W, H, opts };\n    }\n\n    const m = axes(ctx, box, opts);\n    const items = [];\n    if (type === \"lines\") {\n      (opts.series || []).forEach((s, i) => {\n        const color = s.color || PALETTE[i % PALETTE.length];\n        stroke(ctx, (s.points || []).map((p) => [m.sx(p[0]), m.sy(p[1])]), color, 2.2, s.dash);\n        if (s.name) items.push({ label: s.name, color });\n      });\n    } else if (type === \"points\") {\n      ctx.save(); ctx.fillStyle = opts.color || style.MAIN;\n      (opts.points || []).forEach((p) => { ctx.beginPath(); ctx.arc(m.sx(p[0]), m.sy(p[1]), opts.r || 3.6, 0, Math.PI * 2); ctx.fill(); });\n      ctx.restore();\n    } else {\n      const fns = opts.fns || (typeof opts.fn === \"function\" ? [{ fn: opts.fn, color: opts.color, label: opts.label }] : []);\n      fns.forEach((f, i) => {\n        const color = f.color || PALETTE[i % PALETTE.length];\n        stroke(ctx, fnPoints(f.fn, opts, m), color, f.width || 2.4, f.dash);\n        if (f.label) items.push({ label: f.label, color });\n      });\n    }\n    legend(ctx, box, items);\n    return { ctx, sx: m.sx, sy: m.sy, box, W, H, opts };\n  }\n\n  // ---- overlays (world coordinates) ----\n  function marker(ch, x, y, o) {\n    o = o || {};\n    const px = ch.sx(x), py = ch.sy(y);\n    ch.ctx.save();\n    ch.ctx.fillStyle = o.color || style.MAIN;\n    ch.ctx.beginPath(); ch.ctx.arc(px, py, o.r || 5, 0, Math.PI * 2); ch.ctx.fill();\n    ch.ctx.strokeStyle = style.PAPER; ch.ctx.lineWidth = 1.5; ch.ctx.stroke();\n    if (o.label) {\n      ch.ctx.font = font(o.size || 13); ch.ctx.fillStyle = o.color || style.MAIN; ch.ctx.textAlign = \"left\";\n      ch.ctx.fillText(o.label, px + 8, py - 8);\n    }\n    ch.ctx.restore();\n  }\n\n  function vline(ch, x, o) {\n    o = o || {};\n    const px = ch.sx(x);\n    stroke(ch.ctx, [[px, ch.box.y0], [px, ch.box.y1]], o.color || style.MUTED, o.width || 1.2, o.dash || [4, 4]);\n    if (o.label) note(ch, x, null, o.label, { color: o.color || style.MUTED, px: px + 4, py: ch.box.y0 + 12, align: \"left\", size: o.size });\n  }\n\n  function hline(ch, y, o) {\n    o = o || {};\n    const py = ch.sy(y);\n    stroke(ch.ctx, [[ch.box.x0, py], [ch.box.x1, py]], o.color || style.MUTED, o.width || 1.2, o.dash || [4, 4]);\n    if (o.label) note(ch, null, y, o.label, { color: o.color || style.MUTED, px: ch.box.x1 - 4, py: py - 5, align: \"right\", size: o.size });\n  }\n\n  function segment(ch, a, b, o) {\n    o = o || {};\n    const x1 = ch.sx(a[0]), y1 = ch.sy(a[1]), x2 = ch.sx(b[0]), y2 = ch.sy(b[1]);\n    const color = o.color || style.AUX;\n    stroke(ch.ctx, [[x1, y1], [x2, y2]], color, o.width || 2, o.dash);\n    if (o.arrow) {\n      const ang = Math.atan2(y2 - y1, x2 - x1), L = 9;\n      ch.ctx.save(); ch.ctx.fillStyle = color; ch.ctx.beginPath();\n      ch.ctx.moveTo(x2, y2);\n      ch.ctx.lineTo(x2 - L * Math.cos(ang - 0.4), y2 - L * Math.sin(ang - 0.4));\n      ch.ctx.lineTo(x2 - L * Math.cos(ang + 0.4), y2 - L * Math.sin(ang + 0.4));\n      ch.ctx.fill(); ch.ctx.restore();\n    }\n  }\n\n  function note(ch, x, y, text, o) {\n    o = o || {};\n    const px = o.px != null ? o.px : ch.sx(x), py = o.py != null ? o.py : ch.sy(y);\n    ch.ctx.save();\n    ch.ctx.font = font(o.size || 14, o.italic);\n    ch.ctx.fillStyle = o.color || style.INK;\n    ch.ctx.textAlign = o.align || \"left\";\n    ch.ctx.fillText(text, px, py);\n    ch.ctx.restore();\n  }\n\n  /** Pointer probe in world coords; the caller redraws and updates readouts. */\n  function attachProbe(canvas, opts, onProbe, onLeave) {\n    const [x0, x1] = opts.xlim;\n    const [y0, y1] = opts.ylim;\n    const toWorld = (px, py) => {\n      const r = canvas.getBoundingClientRect();\n      const box = plotBox(r.width, r.height, opts);\n      const t = (px - r.left - box.x0) / (box.x1 - box.x0);\n      const u = 1 - (py - r.top - box.y0) / (box.y1 - box.y0);\n      return { x: x0 + t * (x1 - x0), y: y0 + u * (y1 - y0), inside: t >= 0 && t <= 1 };\n    };\n    const handler = (e) => {\n      const p = toWorld(e.clientX, e.clientY);\n      if (p.inside) onProbe(p);\n    };\n    canvas.addEventListener(\"pointermove\", handler);\n    canvas.addEventListener(\"pointerdown\", handler);\n    if (onLeave) canvas.addEventListener(\"pointerleave\", onLeave);\n    canvas.style.touchAction = \"none\";\n    return toWorld;\n  }\n\n  window.HandChart = { render, marker, vline, hline, segment, note, attachProbe, style, font: HAND };\n})();\n</script>\n  <meta charset=\"utf-8\">\n  <style>\n    html, body { margin: 0; height: 100%; background: #faf8f3; font-family: \"PingFang SC\", \"Hiragino Sans GB\", sans-serif; color: #333; overflow: hidden; }\n    #wrap { position: absolute; inset: 0; display: flex; flex-direction: column; }\n    #plot { flex: 1; min-height: 0; margin: 10px 10px 4px; border: 1px solid #d9d2c3; border-radius: 12px; background: #fbfaf6; cursor: crosshair; touch-action: none; }\n    #readout { display: flex; justify-content: center; align-items: center; gap: 28px; padding: 4px 0 8px; font-size: 12px; color: #666; }\n    #readout .k { display: block; font-size: 11px; letter-spacing: .04em; }\n    #readout .v { display: block; font-family: ui-monospace, Menlo, monospace; font-size: 14px; font-weight: 700; color: #c0392b; min-width: 64px; text-align: center; }\n    #formula { display: inline-block; padding: 3px 10px; border: 1px solid #d9d2c3; border-radius: 6px; background: #fff; font-family: Georgia, \"Times New Roman\", serif; font-style: italic; font-size: 13px; color: #333; }\n    #slider-row { display: flex; justify-content: center; align-items: center; gap: 12px; padding: 0 0 8px; font-size: 12px; color: #666; }\n    #slider-row input[type=range] { width: 200px; accent-color: #c0392b; }\n    #slider-row .val { font-family: ui-monospace, Menlo, monospace; font-weight: 700; color: #c0392b; min-width: 40px; text-align: center; }\n  </style>\n</head>\n<body>\n  <div id=\"wrap\">\n    <canvas id=\"plot\"></canvas>\n    <div id=\"readout\">\n      <span><span class=\"k\">斜率 k</span><span class=\"v\" id=\"k-display\">—</span></span>\n      <span><span class=\"k\">误差 |p(0.5)-sin(0.5)|</span><span class=\"v\" id=\"error-display\">—</span></span>\n      <span id=\"formula\">p(x) = k·x</span>\n    </div>\n    <div id=\"slider-row\">\n      <label for=\"k-slider\">k</label>\n      <input type=\"range\" id=\"k-slider\" min=\"0\" max=\"1.5\" step=\"0.001\" value=\"0.5\">\n      <span class=\"val\" id=\"k-slider-val\">0.500</span>\n    </div>\n  </div>\n  <script>\n    // --- the thing being explored -------------------------------------\n    var X_MIN = -1.5, X_MAX = 1.5, Y_MIN = -1.5, Y_MAX = 1.5;\n    var SIN_HALF = Math.sin(0.5); // ≈ 0.4794255386\n    var k = 0.5;\n    var probeX = 0.5;\n\n    // --- plumbing ------------------------------------------------------\n    var canvas = document.getElementById('plot');\n    var ctx = canvas.getContext('2d');\n    var W = 0, H = 0;\n    var PAD = { l: 34, r: 18, t: 18, b: 26 };\n    var sx = function (x) { return PAD.l + (x - X_MIN) / (X_MAX - X_MIN) * (W - PAD.l - PAD.r); };\n    var sy = function (y) { return PAD.t + (Y_MAX - y) / (Y_MAX - Y_MIN) * (H - PAD.t - PAD.b); };\n    var ux = function (px) { return X_MIN + (px - PAD.l) / (W - PAD.l - PAD.r) * (X_MAX - X_MIN); };\n\n    function resize() {\n      var r = canvas.getBoundingClientRect();\n      W = r.width; H = r.height;\n      var dpr = window.devicePixelRatio || 1;\n      canvas.width = Math.round(W * dpr); canvas.height = Math.round(H * dpr);\n      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n      draw();\n    }\n\n    function curve(fn, style) {\n      ctx.save(); Object.assign(ctx, style.props || {});\n      if (style.dash) ctx.setLineDash(style.dash);\n      ctx.beginPath();\n      var n = 1600; var started = false;\n      for (var i = 0; i <= n; i++) {\n        var x = X_MIN + (X_MAX - X_MIN) * i / n, y = fn(x);\n        if (!isFinite(y)) { started = false; continue; }\n        var px = sx(x), py = sy(Math.max(Y_MIN * 1.3, Math.min(Y_MAX * 1.3, y)));\n        started ? ctx.lineTo(px, py) : ctx.moveTo(px, py); started = true;\n      }\n      ctx.stroke(); ctx.restore();\n    }\n\n    function label(text, x, y, color, align) {\n      ctx.save(); ctx.font = 'italic 12px Georgia, serif'; ctx.fillStyle = color;\n      ctx.textAlign = align || 'left'; ctx.fillText(text, x, y); ctx.restore();\n    }\n\n    function draw() {\n      ctx.clearRect(0, 0, W, H);\n\n      // --- axes -------------------------------------------------------\n      ctx.save(); ctx.strokeStyle = '#b9b3a6'; ctx.lineWidth = 1;\n      ctx.beginPath();\n      ctx.moveTo(PAD.l, sy(0)); ctx.lineTo(W - PAD.r, sy(0));\n      ctx.moveTo(sx(0), PAD.t); ctx.lineTo(sx(0), H - PAD.b);\n      ctx.stroke();\n      // ticks\n      ctx.fillStyle = '#8a8377'; ctx.font = 'italic 11px Georgia, serif';\n      ctx.textAlign = 'center';\n      var xticks = [-1, -0.5, 0.5, 1];\n      for (var i = 0; i < xticks.length; i++) {\n        var xv = xticks[i];\n        ctx.beginPath(); ctx.moveTo(sx(xv), sy(0) - 3); ctx.lineTo(sx(xv), sy(0) + 3); ctx.stroke();\n        ctx.fillText(xv, sx(xv), sy(0) + 14);\n      }\n      ctx.textAlign = 'right';\n      var yticks = [-1, -0.5, 0.5, 1];\n      for (var j = 0; j < yticks.length; j++) {\n        var yv = yticks[j];\n        ctx.beginPath(); ctx.moveTo(sx(0) - 3, sy(yv)); ctx.lineTo(sx(0) + 3, sy(yv)); ctx.stroke();\n        ctx.fillText(yv, sx(0) - 6, sy(yv) + 4);\n      }\n      ctx.textAlign = 'left';\n      ctx.fillText('x', W - PAD.r - 2, sy(0) - 6);\n      ctx.fillText('y', sx(0) + 6, PAD.t + 8);\n      ctx.restore();\n\n      // --- sine curve (MAIN) ------------------------------------------\n      curve(function (x) { return Math.sin(x); }, { props: { strokeStyle: '#c0392b', lineWidth: 2 } });\n      label('y = sin x', sx(1.05), sy(Math.sin(1.05)) - 10, '#c0392b');\n\n      // --- line p(x) = kx (AUX) ---------------------------------------\n      curve(function (x) { return k * x; }, { props: { strokeStyle: '#2e8b6f', lineWidth: 2 }, dash: [5, 4] });\n      label('p(x) = kx', sx(1.05), sy(k * 1.05) + 16, '#2e8b6f');\n\n      // --- origin mark (MUTED) ----------------------------------------\n      ctx.save();\n      ctx.strokeStyle = '#8a8377'; ctx.lineWidth = 1.2;\n      ctx.beginPath(); ctx.arc(sx(0), sy(0), 3.5, 0, Math.PI * 2); ctx.stroke();\n      ctx.restore();\n\n      // --- probe at x = 0.5 -------------------------------------------\n      var px = sx(probeX);\n      var ySin = Math.sin(probeX);\n      var yLine = k * probeX;\n      var pySin = sy(ySin);\n      var pyLine = sy(yLine);\n\n      // vertical dashed probe line\n      ctx.save();\n      ctx.strokeStyle = 'rgba(192,57,43,.45)';\n      ctx.setLineDash([3, 4]); ctx.lineWidth = 1;\n      ctx.beginPath(); ctx.moveTo(px, PAD.t); ctx.lineTo(px, H - PAD.b); ctx.stroke();\n      ctx.restore();\n\n      // error segment (vertical line between the two points)\n      ctx.save();\n      ctx.strokeStyle = '#333'; ctx.lineWidth = 1.8;\n      ctx.beginPath(); ctx.moveTo(px, pySin); ctx.lineTo(px, pyLine); ctx.stroke();\n      ctx.restore();\n\n      // point on sine curve (filled red)\n      ctx.save();\n      ctx.fillStyle = '#c0392b';\n      ctx.beginPath(); ctx.arc(px, pySin, 4, 0, Math.PI * 2); ctx.fill();\n      ctx.restore();\n\n      // point on line (filled green)\n      ctx.save();\n      ctx.fillStyle = '#2e8b6f';\n      ctx.beginPath(); ctx.arc(px, pyLine, 4, 0, Math.PI * 2); ctx.fill();\n      ctx.restore();\n\n      // small labels near points\n      label('sin(0.5)', px + 8, pySin - 6, '#c0392b');\n      label('p(0.5)', px + 8, pyLine + 14, '#2e8b6f');\n\n      // --- readouts ---------------------------------------------------\n      document.getElementById('k-display').textContent = k.toFixed(3);\n      document.getElementById('error-display').textContent = Math.abs(yLine - ySin).toFixed(4);\n      document.getElementById('k-slider-val').textContent = k.toFixed(3);\n    }\n\n    // --- pointer interaction (probe follows x) -------------------------\n    function onPointer(e) {\n      var r = canvas.getBoundingClientRect();\n      probeX = Math.max(X_MIN, Math.min(X_MAX, ux(e.clientX - r.left)));\n      draw();\n    }\n    canvas.addEventListener('pointermove', onPointer);\n    canvas.addEventListener('pointerdown', onPointer);\n\n    // --- slider --------------------------------------------------------\n    var slider = document.getElementById('k-slider');\n    slider.addEventListener('input', function () {\n      k = parseFloat(slider.value);\n      draw();\n    });\n\n    // --- teacher-driven control hook -----------------------------------\n    window.hkControl = {\n      set: function (s) {\n        if (!s) return;\n        if (s.k != null) {\n          k = Math.max(0, Math.min(1.5, +s.k));\n          slider.value = k;\n        }\n        if (s.probeX != null) {\n          probeX = Math.max(X_MIN, Math.min(X_MAX, +s.probeX));\n        }\n        draw();\n      },\n      highlight: function (sel) {\n        var el = document.querySelector(sel);\n        if (!el) return;\n        el.style.outline = '2px solid #c0392b';\n        setTimeout(function () { el.style.outline = ''; }, 1500);\n      },\n      annotate: function () {},\n      reveal: function () {}\n    };\n\n    window.addEventListener('resize', resize);\n    resize();\n  </script>\n</body>\n</html>",
        "html_path": null,
        "mermaid": null,
        "layout": "newcol",
        "params": [],
        "controls": []
      },
      "widget_hint": "拖动滑块调节 $k$，让直线在原点附近与曲线最接近。"
    }
  ]
}