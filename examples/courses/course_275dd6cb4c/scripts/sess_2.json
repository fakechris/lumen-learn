{
  "session_id": "sess_2",
  "course_id": "course_275dd6cb4c",
  "title": "全概率公式：分情况加权求和",
  "learning_goal": "学生能识别完备划分，并利用全概率公式将复杂事件概率分解为条件概率的加权和。",
  "steps": [
    {
      "title": "先猜：随机一人阳性概率",
      "beat": "poe",
      "spoken_text": "先别急着算。看第一行：患病率百分之一，检测灵敏度百分之九十九，误报率百分之二。现在随机抓一个人来测，他阳性的概率是多少？第二行我留了个空格让你先猜。很多人的第一反应是「灵敏度九十九，那阳性率差不多九十九吧」，也有人觉得「患病才百分之一，阳性率肯定很低」。你先凭直觉选一个。",
      "boards": [
        {
          "title": "先猜",
          "markdown": "- 患病率 $P(病)=1\\%$\n- 灵敏度 $P(阳 \\mid 病)=99\\%$\n- 误报率 $P(阳 \\mid 健康)=2\\%$\n- 随机一人阳性概率 $P(阳)=$ **?**\n  - 先猜，再算",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "P(阳)=$ **?**",
          "board_index": 0,
          "trigger_phrase": "他阳性的概率是多少",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "凭直觉，随机一人检测阳性的概率大概是多少？",
        "options": [
          "接近 99%，灵敏度那么高",
          "接近 1%，得病的人本来就少",
          "说不准，得把两种情况合起来算"
        ],
        "correct_index": 2,
        "misconceptions": [
          "灵敏度是「得病的人里查出阳性的比例」，不是「所有人里阳性的比例」，分母不一样。",
          "患病率低确实会拉低总阳性率，但误报那 2% 乘上 99% 的健康人也不小，不能只看一头。",
          null
        ],
        "explanation": "总阳性率要把「真病人被查出」和「健康人被误报」两条路合起来，光看一个比例都不对。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "班级平均分的秘密",
      "beat": "analogy",
      "spoken_text": "换个你熟的场景。看板书第一行：一个班男生均分九十，女生均分八十，全班平均分是多少？有人脱口而出八十五，那是把两个数简单平均了。但第二行写着，男生只占四分之一，女生占四分之三。权重是占比，不是人数相等。所以真正的平均分要往女生那边偏。你看最后一行，男生那项乘四分之一，女生那项乘四分之三。",
      "boards": [
        {
          "title": "不能简单平均",
          "markdown": "- 男生均分 $90$，女生均分 $80$\n- 男生占比 $\\tfrac{1}{4}$，女生占比 $\\tfrac{3}{4}$\n- 全班均分 $= 90 \\times \\tfrac{1}{4} + 80 \\times \\tfrac{3}{4}$\n- 权重 = **各群体占比**，不是人数相等",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "highlight",
          "snippet": "90 \\times \\tfrac{1}{4} + 80 \\times \\tfrac{3}{4}",
          "board_index": 0,
          "trigger_phrase": "男生那项乘四分之一，女生那项乘四分之三",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "男生九十、女生八十，男生占四分之一，全班平均分是多少？",
        "options": [
          "八十五，两个数取中间",
          "八十二点五，往女生那边偏",
          "九十，男生分高就按男生算"
        ],
        "correct_index": 1,
        "misconceptions": [
          "简单平均默认两组人数一样多，可这里男生只占四分之一，权重不相等。",
          "只看分高的那一组等于把权重全给了男生，女生那四分之三的人被丢掉了。",
          null
        ],
        "explanation": "九十乘四分之一加八十乘四分之三等于二十二点五加六十，正好八十二点五。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "全概率公式",
      "beat": "define",
      "spoken_text": "把刚才两件事合成一句话。看板书第一行：B1 到 Bn 把整个样本空间切成互斥的几块，谁也不重叠，合起来又盖满全部。第二行就是全概率公式，P(A) 等于每一项 P(A 竖线 Bi) 乘 P(Bi) 再求和。我圈出来的这个 P(Bi)，就是刚才的占比，它当权重。回看上一段，男生女生就是两块 B，阳性率那道题里，病人和健康人也是两块 B。",
      "boards": [
        {
          "title": "分情况加权求和",
          "markdown": "- $B_1,\\ldots,B_n$：**互斥**且**完备**（盖满样本空间）\n- $P(A) = \\sum_i P(A \\mid B_i)\\, P(B_i)$\n- 权重 $P(B_i)$ = 第 $i$ 块情况发生的概率\n- 先分情况 → 再按权重加",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "P(B_i)",
          "board_index": 0,
          "trigger_phrase": "我圈出来的这个 P(Bi)",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "拆开 A，再拼回去",
      "beat": "derive",
      "spoken_text": "回看上一段的板书，公式已经摆在那儿了，但凭什么成立？看这一行：A 被 B1 到 Bn 切成了好几块。A 交 B1、A 交 B2，一直到 A 交 Bn，这些小块彼此不重叠，拼起来正好是 A。所以第一行到第二行，就是把 A 拆成这些小块再求和。",
      "boards": [
        {
          "title": "为什么能拆",
          "markdown": "- $A = (A \\cap B_1) \\cup (A \\cap B_2) \\cup \\cdots \\cup (A \\cap B_n)$\n- 小块**互斥** → $P(A) = \\sum_i P(A \\cap B_i)$\n- 乘法公式：$P(A \\cap B_i) = P(A \\mid B_i)\\, P(B_i)$\n- 代回 → $P(A) = \\sum_i P(A \\mid B_i)\\, P(B_i)$",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "P(A \\cap B_i) = P(A \\mid B_i)\\, P(B_i)",
          "board_index": 0,
          "trigger_phrase": null,
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "把 A 拆成 A 交 B1、A 交 B2 这些小块，为什么可以直接把概率加起来？",
        "options": [
          "因为小块之间不重叠，加起来不会重复算",
          "因为 B1 到 Bn 概率都相等，加起来正好是一"
        ],
        "correct_index": 0,
        "misconceptions": [
          null,
          "这是把互斥和等概率搞混了：能相加靠的是不重叠，跟各块概率是否相等没关系。"
        ],
        "explanation": "互斥保证没有重叠部分，所以各小块的测度可以直接求和，这是可加性的直接应用。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "把数字塞进去：2.97%",
      "beat": "worked_example",
      "spoken_text": "现在看右边这张图。整个矩形就是全体人口，左边窄窄一条是患病的百分之一，右边宽宽一大块是健康的百分之九十九。左边这块里涂了百分之九十九表示阳性，右边这块里涂了百分之二表示误报。把两处涂色面积加起来，就是总阳性率。算一下：百分之九十九乘百分之一，加上百分之二乘百分之九十九，约等于百分之二点九七。",
      "boards": [
        {
          "title": "代入数字",
          "markdown": "- 患病支：$99\\% \\times 1\\% = 0.99\\%$\n- 健康支：$2\\% \\times 99\\% = 1.98\\%$\n- 相加 → $P(阳) \\approx 2.97\\%$",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "highlight",
          "snippet": "2\\% \\times 99\\%",
          "board_index": 0,
          "trigger_phrase": "右边这块里涂了百分之二表示误报",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "别把加权当平均",
      "beat": "contrast",
      "spoken_text": "看这张对比表，左边一列是错误做法，右边一列是正确做法。错误做法把百分之九十九和百分之二直接加起来除以二，得到百分之五十点五，这个数大得离谱。差别就在中间那一列：正确做法每一项都乘上了它自己的权重，患病那一项乘百分之一，健康那一项乘百分之九十九。权重不一样，结果就天差地别。",
      "boards": [
        {
          "title": "平均 vs 加权",
          "markdown": "| 做法 | 算式 | 结果 |\n| --- | --- | --- |\n| 直接平均 | $(99\\% + 2\\%) / 2$ | $50.5\\%$ |\n| 全概率加权 | $99\\% \\times 1\\% + 2\\% \\times 99\\%$ | $2.97\\%$ |",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "spotlight",
          "snippet": "$99\\% \\times 1\\% + 2\\% \\times 99\\%$",
          "board_index": 0,
          "trigger_phrase": "正确做法每一项都乘上了它自己的权重",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "回顾：三步走",
      "beat": "recap",
      "spoken_text": "最后把整节课收成三行。看板书第一行：先找一组互斥又完备的划分，就像把人群切成患病和健康两块。第二行：每一块都要两个数，条件概率和它自己的权重。第三行：把每块的乘积加起来，不是平均。回看开头那个百分之一、百分之九十九、百分之二，现在你能自己走完这三步了吗？",
      "boards": [
        {
          "title": "三步走",
          "markdown": "- 第一步：找**互斥且完备**的划分 $B_1,\\ldots,B_n$\n- 第二步：每块求 $P(A \\mid B_i)$ 和权重 $P(B_i)$\n- 第三步：加权求和 $P(A)=\\sum_i P(A \\mid B_i)P(B_i)$\n- 权重不同 → **不是简单平均**",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "P(A)=\\sum_i P(A \\mid B_i)P(B_i)",
          "board_index": 0,
          "trigger_phrase": "把每块的乘积加起来",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": {
        "title": "全概率公式",
        "description": "先找互斥完备的划分，再对每块的条件概率按该块自身的概率加权求和，而不是简单平均。"
      },
      "new_page_title": null
    }
  ],
  "exercises": [
    {
      "exercise_id": "sess_2_ex1",
      "kind": "fill_blank",
      "stem": "在全概率公式 $P(A) = \\sum_i P(A \\mid B_i)\\,P(B_i)$ 中，$P(B_i)$ 被称为第 $i$ 块情况的 ____。",
      "options": [],
      "correct_index": null,
      "answer": "权重",
      "accepted": [
        "权",
        "占比",
        "概率权重",
        "加权系数"
      ],
      "explanation": "全概率公式的本质是加权求和，$P(B_i)$ 衡量第 $i$ 种情况在总体中的相对重要性，因此称为权重。它决定了每个条件概率 $P(A \\mid B_i)$ 对最终结果的贡献大小。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_2_ex2",
      "kind": "single_choice",
      "stem": "某工厂有甲、乙两条生产线，甲线产量占 $60\\%$，次品率为 $2\\%$；乙线产量占 $40\\%$，次品率为 $5\\%$。随机抽取一件产品，它是次品的概率为（　　）。",
      "options": [
        "$3.5\\%$",
        "$3.2\\%$",
        "$2.8\\%$",
        "$7\\%$"
      ],
      "correct_index": 1,
      "answer": null,
      "accepted": [],
      "explanation": "按全概率公式，以生产线为划分：$P(次品) = P(次品 \\mid 甲)P(甲) + P(次品 \\mid 乙)P(乙) = 2\\% \\times 60\\% + 5\\% \\times 40\\% = 1.2\\% + 2\\% = 3.2\\%$。选项 A 是直接平均 $(2\\%+5\\%)/2=3.5\\%$，选项 C 是错误交换权重，选项 D 是忽略权重直接相加。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_2_ex3",
      "kind": "single_choice",
      "stem": "关于全概率公式中的划分 $B_1, B_2, \\ldots, B_n$，下列说法正确的是（　　）。",
      "options": [
        "各 $B_i$ 可以相交，只要并集为全集即可",
        "各 $B_i$ 必须互斥，但不必盖满整个样本空间",
        "各 $B_i$ 必须互斥且并集为整个样本空间",
        "各 $B_i$ 必须等概率，才能使用全概率公式"
      ],
      "correct_index": 2,
      "answer": null,
      "accepted": [],
      "explanation": "全概率公式要求 $B_1, \\ldots, B_n$ 构成样本空间的一个划分，即两两互斥（不重叠）且并集为全集（盖满）。互斥保证 $A \\cap B_i$ 互斥，从而概率可加；完备保证 $A$ 被完全分解。等概率不是必要条件，权重可以不同。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_2_ex4",
      "kind": "interactive",
      "stem": "通过调节患病率滑块，观察患病支与健康支对总阳性率的贡献变化，然后回答问题。",
      "options": [
        "总阳性率始终约为 $2.97\\%$，与患病率无关",
        "患病率越高，总阳性率越接近 $99\\%$",
        "患病率越低，总阳性率越接近 $2\\%$",
        "总阳性率等于灵敏度与误报率的简单平均"
      ],
      "correct_index": 1,
      "answer": null,
      "accepted": [],
      "explanation": "总阳性率 $P(阳) = 99\\% \\times P(病) + 2\\% \\times (1-P(病))$。当患病率 $P(病)$ 增大时，患病支的权重增大，总阳性率趋近 $99\\%$；当患病率极低时，健康支权重接近 $1$，总阳性率趋近 $2\\%$。因此 B 正确，C 方向相反，A 只在患病率为 $1\\%$ 时成立，D 忽略了权重。",
      "widget": {
        "kind": "explorable",
        "title": "阳性率分解：患病率滑块",
        "task": "拖动“患病率”滑块（范围 $0.1\\%$ 到 $10\\%$），观察左侧患病支和右侧健康支的涂色面积如何变化。灵敏度固定为 $99\\%$，误报率固定为 $2\\%$。注意总阳性率随患病率如何变化。",
        "html": "<!DOCTYPE html>\n<html>\n<head><script>/**\n * HandChart — zero-dependency hand-drawn chart base for explorable widgets.\n *\n * Injected into every generated 2D explorable (widget_generator.py) so the\n * model supplies data + overlays instead of hand-writing axes. The look matches\n * the handwritten page: paper panel, slightly wobbly ink axes, main red /\n * aux green, handwriting labels.\n *\n * API (all coordinates in WORLD units unless noted):\n *   const ch = HandChart.render(canvas, opts)\n *     opts = { type: \"function\"|\"points\"|\"bars\"|\"lines\",\n *              xlim:[x0,x1], ylim:[y0,y1],                  (all but bars)\n *              fn: x=>y | fns:[{fn, color?, label?}],        (function)\n *              points:[[x,y],...],                          (points)\n *              series:[{name, points, color?, dash?}],      (lines; legend drawn)\n *              bars:{labels:[...], values:[...], highlight?} (bars)\n *              xlabel, ylabel, grid:true|false, title }\n *     returns { ctx, sx, sy, box, W, H, opts }  — keep it to draw overlays\n *   HandChart.marker(ch, x, y, {color, label, r})       dot + optional label\n *   HandChart.vline(ch, x, {color, dash, label})         vertical guide\n *   HandChart.hline(ch, y, {color, dash, label})         horizontal guide\n *   HandChart.segment(ch, [x1,y1], [x2,y2], {color, width, dash, arrow})\n *   HandChart.note(ch, x, y, text, {color, align, size}) handwriting annotation\n *   HandChart.attachProbe(canvas, {xlim, ylim}, onProbe, onLeave?)\n *     pointer follow; onProbe({x, y}) in world coords — redraw + readouts there.\n *   HandChart.style = { INK, AXIS, MAIN, AUX, GRID, PAPER, MUTED }\n */\n(function () {\n  const style = {\n    INK: \"#2b2b2b\", AXIS: \"#8a8377\", MAIN: \"#c0392b\", AUX: \"#2e8b6f\",\n    GRID: \"#ece5d4\", PAPER: \"#fbfaf6\", MUTED: \"#8a8377\", PURPLE: \"#7c3aed\", BLUE: \"#2563eb\",\n  };\n  const PALETTE = [style.MAIN, style.AUX, style.BLUE, style.PURPLE, \"#d97706\"];\n  const PAD = { l: 48, r: 18, t: 18, b: 36 };\n  const HAND = \"'Patrick Hand', 'LXGW WenKai Lite', 'PingFang SC', sans-serif\";\n\n  function font(size, italic) { return (italic ? \"italic \" : \"\") + size + \"px \" + HAND; }\n\n  // deterministic wobble so the \"hand\" does not shake on every redraw\n  function wob(seed, amp) { return Math.sin(seed * 12.9898) * amp; }\n\n  function tickVals(min, max, n) {\n    const span = max - min || 1;\n    const raw = span / n;\n    const mag = Math.pow(10, Math.floor(Math.log10(raw)));\n    const step = [1, 2, 2.5, 5, 10].map((m) => m * mag).find((s) => span / s <= n) || mag * 10;\n    const out = [];\n    for (let v = Math.ceil(min / step) * step; v <= max + step * 1e-9; v += step) out.push(+v.toFixed(10));\n    return out;\n  }\n\n  function fmt(v) {\n    const a = Math.abs(v);\n    if (a === 0) return \"0\";\n    if (a >= 10000 || a < 0.01) return v.toExponential(1);\n    return String(+v.toFixed(Math.max(0, 3 - Math.floor(Math.log10(a)))));\n  }\n\n  /** a hand-drawn line: two passes with tiny deterministic wobble */\n  function inkLine(ctx, x1, y1, x2, y2, color, width, seed) {\n    ctx.save();\n    ctx.strokeStyle = color; ctx.lineWidth = width; ctx.lineCap = \"round\";\n    const n = 6, s = seed || (x1 + y2);\n    ctx.beginPath();\n    for (let i = 0; i <= n; i++) {\n      const t = i / n, x = x1 + (x2 - x1) * t, y = y1 + (y2 - y1) * t;\n      const w = i === 0 || i === n ? 0 : wob(s + i, 0.7);\n      i ? ctx.lineTo(x + w, y + w) : ctx.moveTo(x, y);\n    }\n    ctx.stroke();\n    ctx.restore();\n  }\n\n  function frame(ctx, W, H) {\n    ctx.clearRect(0, 0, W, H);\n    ctx.save();\n    ctx.fillStyle = style.PAPER;\n    ctx.fillRect(0, 0, W, H);\n    ctx.restore();\n  }\n\n  function plotBox(W, H, opts) {\n    const t = PAD.t + (opts && opts.title ? 18 : 0);\n    return { x0: PAD.l, y0: t, x1: W - PAD.r, y1: H - PAD.b };\n  }\n\n  function mapping(box, xlim, ylim) {\n    const [x0, x1] = xlim, [y0, y1] = ylim;\n    return {\n      sx: (v) => box.x0 + (v - x0) / (x1 - x0) * (box.x1 - box.x0),\n      sy: (v) => box.y1 - (v - y0) / (y1 - y0) * (box.y1 - box.y0),\n    };\n  }\n\n  function axes(ctx, box, opts) {\n    const [x0, x1] = opts.xlim, [y0, y1] = opts.ylim;\n    const { sx, sy } = mapping(box, opts.xlim, opts.ylim);\n    const xt = tickVals(x0, x1, 6).filter((v) => v >= x0 && v <= x1);\n    const yt = tickVals(y0, y1, 4).filter((v) => v >= y0 && v <= y1);\n    if (opts.grid !== false) {\n      ctx.save(); ctx.strokeStyle = style.GRID; ctx.lineWidth = 1;\n      for (const v of xt) { ctx.beginPath(); ctx.moveTo(sx(v), box.y0); ctx.lineTo(sx(v), box.y1); ctx.stroke(); }\n      for (const v of yt) { ctx.beginPath(); ctx.moveTo(box.x0, sy(v)); ctx.lineTo(box.x1, sy(v)); ctx.stroke(); }\n      ctx.restore();\n    }\n    // axes sit on zero when zero is inside the range, else on the box edge\n    const ax = y0 <= 0 && y1 >= 0 ? sy(0) : box.y1;\n    const ay = x0 <= 0 && x1 >= 0 ? sx(0) : box.x0;\n    inkLine(ctx, box.x0 - 4, ax, box.x1 + 6, ax, style.AXIS, 1.4, 1);\n    inkLine(ctx, ay, box.y1 + 4, ay, box.y0 - 6, style.AXIS, 1.4, 2);\n    // arrow heads\n    ctx.save(); ctx.fillStyle = style.AXIS;\n    ctx.beginPath(); ctx.moveTo(box.x1 + 8, ax); ctx.lineTo(box.x1 + 1, ax - 3.5); ctx.lineTo(box.x1 + 1, ax + 3.5); ctx.fill();\n    ctx.beginPath(); ctx.moveTo(ay, box.y0 - 8); ctx.lineTo(ay - 3.5, box.y0 - 1); ctx.lineTo(ay + 3.5, box.y0 - 1); ctx.fill();\n    ctx.restore();\n    ctx.save();\n    ctx.fillStyle = style.MUTED; ctx.font = font(12);\n    ctx.textAlign = \"center\";\n    for (const v of xt) {\n      if (Math.abs(sx(v) - ay) < 1 && ay !== box.x0) continue; // skip the label under the y axis\n      inkLine(ctx, sx(v), ax - 3, sx(v), ax + 3, style.AXIS, 1, v);\n      ctx.fillText(fmt(v), sx(v), ax + 16);\n    }\n    ctx.textAlign = \"right\";\n    for (const v of yt) {\n      if (Math.abs(sy(v) - ax) < 1 && ax !== box.y1) continue;\n      inkLine(ctx, ay - 3, sy(v), ay + 3, sy(v), style.AXIS, 1, v + 7);\n      ctx.fillText(fmt(v), ay - 7, sy(v) + 4);\n    }\n    ctx.font = font(14, true); ctx.fillStyle = style.INK;\n    if (opts.xlabel) { ctx.textAlign = \"right\"; ctx.fillText(opts.xlabel, box.x1 + 4, ax - 8); }\n    if (opts.ylabel) { ctx.textAlign = \"left\"; ctx.fillText(opts.ylabel, ay + 8, box.y0 + 4); }\n    ctx.restore();\n    return { sx, sy };\n  }\n\n  function stroke(ctx, pts, color, width, dash) {\n    if (!pts.length) return;\n    ctx.save();\n    ctx.strokeStyle = color; ctx.lineWidth = width || 2.2;\n    ctx.lineJoin = \"round\"; ctx.lineCap = \"round\";\n    if (dash) ctx.setLineDash(dash);\n    ctx.beginPath();\n    let pen = false;\n    for (const p of pts) {\n      if (!p) { pen = false; continue; }\n      pen ? ctx.lineTo(p[0], p[1]) : ctx.moveTo(p[0], p[1]);\n      pen = true;\n    }\n    ctx.stroke();\n    ctx.restore();\n  }\n\n  function fnPoints(fn, opts, m) {\n    const [x0, x1] = opts.xlim, [y0, y1] = opts.ylim;\n    const n = 400, pts = [];\n    const margin = (y1 - y0) * 0.02;\n    for (let i = 0; i <= n; i++) {\n      const x = x0 + (x1 - x0) * i / n;\n      let y;\n      try { y = fn(x); } catch (e) { y = NaN; }\n      if (!isFinite(y) || y < y0 - margin || y > y1 + margin) { pts.push(null); continue; }\n      pts.push([m.sx(x), m.sy(y)]);\n    }\n    return pts;\n  }\n\n  function legend(ctx, box, items) {\n    if (!items.length) return;\n    ctx.save();\n    ctx.font = font(13); ctx.textAlign = \"left\"; ctx.textBaseline = \"middle\";\n    let x = box.x0 + 10, y = box.y0 + 12;\n    for (const it of items) {\n      inkLine(ctx, x, y, x + 22, y, it.color, 2.4, x);\n      ctx.fillStyle = style.INK;\n      ctx.fillText(it.label, x + 28, y + 1);\n      x += 28 + ctx.measureText(it.label).width + 18;\n    }\n    ctx.restore();\n  }\n\n  function title(ctx, W, text) {\n    ctx.save();\n    ctx.font = font(15); ctx.fillStyle = style.INK; ctx.textAlign = \"center\";\n    ctx.fillText(text, W / 2, 16);\n    ctx.restore();\n  }\n\n  function render(canvas, opts) {\n    const ctx = canvas.getContext(\"2d\");\n    const rect = canvas.getBoundingClientRect();\n    const W = Math.max(60, Math.round(rect.width)), H = Math.max(60, Math.round(rect.height));\n    const dpr = window.devicePixelRatio || 1;\n    if (canvas.width !== Math.round(W * dpr) || canvas.height !== Math.round(H * dpr)) {\n      canvas.width = Math.round(W * dpr); canvas.height = Math.round(H * dpr);\n    }\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n    frame(ctx, W, H);\n    if (opts.title) title(ctx, W, opts.title);\n    const box = plotBox(W, H, opts);\n    const type = opts.type || \"function\";\n\n    if (type === \"bars\") {\n      const labels = opts.bars.labels || [], values = opts.bars.values || [];\n      const n = Math.max(1, values.length);\n      const bw = (box.x1 - box.x0) / n;\n      const vmax = Math.max(...values.map((v) => Math.abs(v)), 0) * 1.15 || 1;\n      const base = box.y1;\n      inkLine(ctx, box.x0 - 4, base, box.x1 + 4, base, style.AXIS, 1.4, 3);\n      ctx.save();\n      values.forEach((v, i) => {\n        const h = (Math.abs(v) / vmax) * (box.y1 - box.y0);\n        const x = box.x0 + i * bw + bw * 0.2, w = bw * 0.6, y = base - h;\n        const hot = opts.bars.highlight != null && i === opts.bars.highlight;\n        ctx.fillStyle = hot ? style.MAIN : \"#e6dfcf\";\n        ctx.fillRect(x, y, w, h);\n        ctx.strokeStyle = hot ? style.MAIN : style.AXIS; ctx.lineWidth = 1.2;\n        ctx.strokeRect(x + 0.5, y + 0.5, w - 1, h - 1);\n        ctx.font = font(13); ctx.textAlign = \"center\"; ctx.fillStyle = style.INK;\n        ctx.fillText(String(labels[i] == null ? i + 1 : labels[i]), x + w / 2, base + 17);\n        ctx.fillStyle = hot ? style.MAIN : style.MUTED;\n        ctx.fillText(fmt(v), x + w / 2, y - 6);\n      });\n      ctx.restore();\n      const sx = (i) => box.x0 + (i + 0.5) * bw, sy = (v) => base - (Math.abs(v) / vmax) * (box.y1 - box.y0);\n      return { ctx, sx, sy, box, W, H, opts };\n    }\n\n    const m = axes(ctx, box, opts);\n    const items = [];\n    if (type === \"lines\") {\n      (opts.series || []).forEach((s, i) => {\n        const color = s.color || PALETTE[i % PALETTE.length];\n        stroke(ctx, (s.points || []).map((p) => [m.sx(p[0]), m.sy(p[1])]), color, 2.2, s.dash);\n        if (s.name) items.push({ label: s.name, color });\n      });\n    } else if (type === \"points\") {\n      ctx.save(); ctx.fillStyle = opts.color || style.MAIN;\n      (opts.points || []).forEach((p) => { ctx.beginPath(); ctx.arc(m.sx(p[0]), m.sy(p[1]), opts.r || 3.6, 0, Math.PI * 2); ctx.fill(); });\n      ctx.restore();\n    } else {\n      const fns = opts.fns || (typeof opts.fn === \"function\" ? [{ fn: opts.fn, color: opts.color, label: opts.label }] : []);\n      fns.forEach((f, i) => {\n        const color = f.color || PALETTE[i % PALETTE.length];\n        stroke(ctx, fnPoints(f.fn, opts, m), color, f.width || 2.4, f.dash);\n        if (f.label) items.push({ label: f.label, color });\n      });\n    }\n    legend(ctx, box, items);\n    return { ctx, sx: m.sx, sy: m.sy, box, W, H, opts };\n  }\n\n  // ---- overlays (world coordinates) ----\n  function marker(ch, x, y, o) {\n    o = o || {};\n    const px = ch.sx(x), py = ch.sy(y);\n    ch.ctx.save();\n    ch.ctx.fillStyle = o.color || style.MAIN;\n    ch.ctx.beginPath(); ch.ctx.arc(px, py, o.r || 5, 0, Math.PI * 2); ch.ctx.fill();\n    ch.ctx.strokeStyle = style.PAPER; ch.ctx.lineWidth = 1.5; ch.ctx.stroke();\n    if (o.label) {\n      ch.ctx.font = font(o.size || 13); ch.ctx.fillStyle = o.color || style.MAIN; ch.ctx.textAlign = \"left\";\n      ch.ctx.fillText(o.label, px + 8, py - 8);\n    }\n    ch.ctx.restore();\n  }\n\n  function vline(ch, x, o) {\n    o = o || {};\n    const px = ch.sx(x);\n    stroke(ch.ctx, [[px, ch.box.y0], [px, ch.box.y1]], o.color || style.MUTED, o.width || 1.2, o.dash || [4, 4]);\n    if (o.label) note(ch, x, null, o.label, { color: o.color || style.MUTED, px: px + 4, py: ch.box.y0 + 12, align: \"left\", size: o.size });\n  }\n\n  function hline(ch, y, o) {\n    o = o || {};\n    const py = ch.sy(y);\n    stroke(ch.ctx, [[ch.box.x0, py], [ch.box.x1, py]], o.color || style.MUTED, o.width || 1.2, o.dash || [4, 4]);\n    if (o.label) note(ch, null, y, o.label, { color: o.color || style.MUTED, px: ch.box.x1 - 4, py: py - 5, align: \"right\", size: o.size });\n  }\n\n  function segment(ch, a, b, o) {\n    o = o || {};\n    const x1 = ch.sx(a[0]), y1 = ch.sy(a[1]), x2 = ch.sx(b[0]), y2 = ch.sy(b[1]);\n    const color = o.color || style.AUX;\n    stroke(ch.ctx, [[x1, y1], [x2, y2]], color, o.width || 2, o.dash);\n    if (o.arrow) {\n      const ang = Math.atan2(y2 - y1, x2 - x1), L = 9;\n      ch.ctx.save(); ch.ctx.fillStyle = color; ch.ctx.beginPath();\n      ch.ctx.moveTo(x2, y2);\n      ch.ctx.lineTo(x2 - L * Math.cos(ang - 0.4), y2 - L * Math.sin(ang - 0.4));\n      ch.ctx.lineTo(x2 - L * Math.cos(ang + 0.4), y2 - L * Math.sin(ang + 0.4));\n      ch.ctx.fill(); ch.ctx.restore();\n    }\n  }\n\n  function note(ch, x, y, text, o) {\n    o = o || {};\n    const px = o.px != null ? o.px : ch.sx(x), py = o.py != null ? o.py : ch.sy(y);\n    ch.ctx.save();\n    ch.ctx.font = font(o.size || 14, o.italic);\n    ch.ctx.fillStyle = o.color || style.INK;\n    ch.ctx.textAlign = o.align || \"left\";\n    ch.ctx.fillText(text, px, py);\n    ch.ctx.restore();\n  }\n\n  /** Pointer probe in world coords; the caller redraws and updates readouts. */\n  function attachProbe(canvas, opts, onProbe, onLeave) {\n    const [x0, x1] = opts.xlim;\n    const [y0, y1] = opts.ylim;\n    const toWorld = (px, py) => {\n      const r = canvas.getBoundingClientRect();\n      const box = plotBox(r.width, r.height, opts);\n      const t = (px - r.left - box.x0) / (box.x1 - box.x0);\n      const u = 1 - (py - r.top - box.y0) / (box.y1 - box.y0);\n      return { x: x0 + t * (x1 - x0), y: y0 + u * (y1 - y0), inside: t >= 0 && t <= 1 };\n    };\n    const handler = (e) => {\n      const p = toWorld(e.clientX, e.clientY);\n      if (p.inside) onProbe(p);\n    };\n    canvas.addEventListener(\"pointermove\", handler);\n    canvas.addEventListener(\"pointerdown\", handler);\n    if (onLeave) canvas.addEventListener(\"pointerleave\", onLeave);\n    canvas.style.touchAction = \"none\";\n    return toWorld;\n  }\n\n  window.HandChart = { render, marker, vline, hline, segment, note, attachProbe, style, font: HAND };\n})();\n</script>\n  <meta charset=\"utf-8\">\n  <style>\n    html, body { margin: 0; height: 100%; background: #faf8f3; font-family: \"PingFang SC\", \"Hiragino Sans GB\", sans-serif; color: #333; overflow: hidden; }\n    #wrap { position: absolute; inset: 0; display: flex; flex-direction: column; }\n    #plot { flex: 1; min-height: 0; margin: 10px 10px 4px; border: 1px solid #d9d2c3; border-radius: 12px; background: #fbfaf6; cursor: crosshair; touch-action: none; }\n    #readout { display: flex; justify-content: center; align-items: center; gap: 28px; padding: 4px 0 8px; font-size: 12px; color: #666; }\n    #readout .k { display: block; font-size: 11px; letter-spacing: .04em; }\n    #readout .v { display: block; font-family: ui-monospace, Menlo, monospace; font-size: 14px; font-weight: 700; color: #c0392b; min-width: 64px; text-align: center; }\n    #formula { display: inline-block; padding: 3px 10px; border: 1px solid #d9d2c3; border-radius: 6px; background: #fff; font-family: Georgia, \"Times New Roman\", serif; font-style: italic; font-size: 13px; color: #333; }\n    #controls { display: flex; justify-content: center; align-items: center; gap: 18px; padding: 0 0 10px; font-size: 12px; color: #666; }\n    #controls label { display: flex; align-items: center; gap: 8px; }\n    #prevalence-slider { width: 180px; accent-color: #c0392b; }\n    #prevalence-display { font-family: ui-monospace, Menlo, monospace; font-weight: 700; color: #c0392b; min-width: 48px; text-align: right; }\n  </style>\n</head>\n<body>\n  <div id=\"wrap\">\n    <canvas id=\"plot\"></canvas>\n    <div id=\"readout\">\n      <span><span class=\"k\">患病率</span><span class=\"v\" id=\"prevalence-display\">—</span></span>\n      <span><span class=\"k\">总阳性率</span><span class=\"v\" id=\"total_positive_rate-display\">—</span></span>\n      <span id=\"formula\">P(+) = P(病)·Se + P(健)·(1−Sp)</span>\n    </div>\n    <div id=\"controls\">\n      <label>患病率 <input type=\"range\" id=\"prevalence-slider\" min=\"0.001\" max=\"0.1\" step=\"0.001\" value=\"0.01\"></label>\n    </div>\n  </div>\n  <script>\n    // --- 固定参数 ------------------------------------------------------\n    const SENSITIVITY = 0.99;      // 灵敏度 99%\n    const FALSE_POSITIVE = 0.02;   // 误报率 2%\n\n    // --- 状态 ----------------------------------------------------------\n    let prevalence = 0.01;         // 当前患病率 (0.001 ~ 0.1)\n\n    // --- 画布与坐标 ----------------------------------------------------\n    const canvas = document.getElementById('plot');\n    const ctx = canvas.getContext('2d');\n    let W = 0, H = 0;\n\n    // 6x6 锚点网格 (A1~F6) 映射到画布坐标\n    // 列 A~F 对应 x 从 0 到 1，行 1~6 对应 y 从 0 到 1 (从上到下)\n    function anchor(col, row) {\n      // col: 'A'~'F' -> 0~5, row: 1~6 -> 0~5\n      const colIdx = col.charCodeAt(0) - 65; // A=0\n      const rowIdx = row - 1;                // 1->0\n      const x = (colIdx + 0.5) / 6 * W;\n      const y = (rowIdx + 0.5) / 6 * H;\n      return { x, y };\n    }\n\n    // 获取锚点区域 (用于绘制矩形)\n    function anchorRect(col, row, w, h) {\n      const a = anchor(col, row);\n      return { x: a.x - w/2, y: a.y - h/2, w, h };\n    }\n\n    // --- 工具函数 ------------------------------------------------------\n    function drawRoundedRect(x, y, w, h, r) {\n      ctx.beginPath();\n      ctx.moveTo(x + r, y);\n      ctx.lineTo(x + w - r, y);\n      ctx.quadraticCurveTo(x + w, y, x + w, y + r);\n      ctx.lineTo(x + w, y + h - r);\n      ctx.quadraticCurveTo(x + w, y + h, x + w - r, y + h);\n      ctx.lineTo(x + r, y + h);\n      ctx.quadraticCurveTo(x, y + h, x, y + h - r);\n      ctx.lineTo(x, y + r);\n      ctx.quadraticCurveTo(x, y, x + r, y);\n      ctx.closePath();\n    }\n\n    function label(text, x, y, color, align, font) {\n      ctx.save();\n      ctx.font = font || 'italic 12px Georgia, serif';\n      ctx.fillStyle = color || '#333';\n      ctx.textAlign = align || 'left';\n      ctx.textBaseline = 'middle';\n      ctx.fillText(text, x, y);\n      ctx.restore();\n    }\n\n    // --- 核心绘制 ------------------------------------------------------\n    function draw() {\n      ctx.clearRect(0, 0, W, H);\n\n      // 计算概率\n      const p_sick = prevalence;\n      const p_healthy = 1 - prevalence;\n      const p_true_pos = p_sick * SENSITIVITY;          // 真阳性\n      const p_false_neg = p_sick * (1 - SENSITIVITY);   // 假阴性\n      const p_false_pos = p_healthy * FALSE_POSITIVE;   // 假阳性\n      const p_true_neg = p_healthy * (1 - FALSE_POSITIVE); // 真阴性\n      const total_positive = p_true_pos + p_false_pos;\n\n      // 更新读数\n      document.getElementById('prevalence-display').textContent = (prevalence * 100).toFixed(1) + '%';\n      document.getElementById('total_positive_rate-display').textContent = (total_positive * 100).toFixed(2) + '%';\n\n      // --- 绘制左侧患病支 (B3) ---\n      const sickRect = anchorRect('B', 3, W * 0.22, H * 0.38);\n      const sickX = sickRect.x, sickY = sickRect.y, sickW = sickRect.w, sickH = sickRect.h;\n\n      // 患病支背景 (浅色)\n      ctx.save();\n      ctx.fillStyle = '#f5f0e8';\n      drawRoundedRect(sickX, sickY, sickW, sickH, 6);\n      ctx.fill();\n      ctx.strokeStyle = '#d9d2c3';\n      ctx.lineWidth = 1;\n      ctx.stroke();\n      ctx.restore();\n\n      // 患病支涂色面积 (按患病率比例)\n      const sickFillH = sickH * (prevalence / 0.1); // 最大10%时填满\n      ctx.save();\n      ctx.fillStyle = 'rgba(192, 57, 43, 0.25)';\n      drawRoundedRect(sickX, sickY + sickH - sickFillH, sickW, sickFillH, 6);\n      ctx.fill();\n      ctx.restore();\n\n      // 患病支内部细分: 真阳性 (上部) 和 假阴性 (下部)\n      const truePosH = sickFillH * SENSITIVITY;\n      const falseNegH = sickFillH * (1 - SENSITIVITY);\n      ctx.save();\n      // 真阳性区域\n      ctx.fillStyle = 'rgba(192, 57, 43, 0.55)';\n      drawRoundedRect(sickX, sickY + sickH - sickFillH, sickW, truePosH, 6);\n      ctx.fill();\n      // 假阴性区域\n      ctx.fillStyle = 'rgba(192, 57, 43, 0.15)';\n      drawRoundedRect(sickX, sickY + sickH - sickFillH + truePosH, sickW, falseNegH, 6);\n      ctx.fill();\n      ctx.restore();\n\n      // 患病支标签\n      label('患病支', sickX + sickW/2, sickY - 12, '#c0392b', 'center', 'bold 13px Georgia, serif');\n      label('P(病) = ' + (prevalence * 100).toFixed(1) + '%', sickX + sickW/2, sickY + sickH + 16, '#c0392b', 'center', 'italic 12px Georgia, serif');\n\n      // 内部标注\n      if (sickFillH > 20) {\n        label('真阳性 ' + (p_true_pos * 100).toFixed(2) + '%', sickX + sickW/2, sickY + sickH - sickFillH + truePosH/2, '#fff', 'center', '11px Georgia, serif');\n        if (falseNegH > 12) {\n          label('假阴性 ' + (p_false_neg * 100).toFixed(2) + '%', sickX + sickW/2, sickY + sickH - sickFillH + truePosH + falseNegH/2, '#c0392b', 'center', '11px Georgia, serif');\n        }\n      }\n\n      // --- 绘制右侧健康支 (E3) ---\n      const healthyRect = anchorRect('E', 3, W * 0.22, H * 0.38);\n      const healthyX = healthyRect.x, healthyY = healthyRect.y, healthyW = healthyRect.w, healthyH = healthyRect.h;\n\n      // 健康支背景\n      ctx.save();\n      ctx.fillStyle = '#f5f0e8';\n      drawRoundedRect(healthyX, healthyY, healthyW, healthyH, 6);\n      ctx.fill();\n      ctx.strokeStyle = '#d9d2c3';\n      ctx.lineWidth = 1;\n      ctx.stroke();\n      ctx.restore();\n\n      // 健康支涂色面积 (按健康率比例)\n      const healthyFillH = healthyH * (p_healthy / 0.999); // 最大99.9%时填满\n      ctx.save();\n      ctx.fillStyle = 'rgba(46, 139, 111, 0.2)';\n      drawRoundedRect(healthyX, healthyY + healthyH - healthyFillH, healthyW, healthyFillH, 6);\n      ctx.fill();\n      ctx.restore();\n\n      // 健康支内部细分: 假阳性 (上部) 和 真阴性 (下部)\n      const falsePosH = healthyFillH * FALSE_POSITIVE;\n      const trueNegH = healthyFillH * (1 - FALSE_POSITIVE);\n      ctx.save();\n      // 假阳性区域\n      ctx.fillStyle = 'rgba(46, 139, 111, 0.55)';\n      drawRoundedRect(healthyX, healthyY + healthyH - healthyFillH, healthyW, falsePosH, 6);\n      ctx.fill();\n      // 真阴性区域\n      ctx.fillStyle = 'rgba(46, 139, 111, 0.15)';\n      drawRoundedRect(healthyX, healthyY + healthyH - healthyFillH + falsePosH, healthyW, trueNegH, 6);\n      ctx.fill();\n      ctx.restore();\n\n      // 健康支标签\n      label('健康支', healthyX + healthyW/2, healthyY - 12, '#2e8b6f', 'center', 'bold 13px Georgia, serif');\n      label('P(健) = ' + (p_healthy * 100).toFixed(1) + '%', healthyX + healthyW/2, healthyY + healthyH + 16, '#2e8b6f', 'center', 'italic 12px Georgia, serif');\n\n      // 内部标注\n      if (healthyFillH > 20) {\n        if (falsePosH > 12) {\n          label('假阳性 ' + (p_false_pos * 100).toFixed(2) + '%', healthyX + healthyW/2, healthyY + healthyH - healthyFillH + falsePosH/2, '#fff', 'center', '11px Georgia, serif');\n        }\n        label('真阴性 ' + (p_true_neg * 100).toFixed(2) + '%', healthyX + healthyW/2, healthyY + healthyH - healthyFillH + falsePosH + trueNegH/2, '#2e8b6f', 'center', '11px Georgia, serif');\n      }\n\n      // --- 固定参数标签 (C1, E1) ---\n      const c1 = anchor('C', 1);\n      label('灵敏度 = 99%', c1.x, c1.y, '#333', 'center', 'italic 12px Georgia, serif');\n      const e1 = anchor('E', 1);\n      label('误报率 = 2%', e1.x, e1.y, '#333', 'center', 'italic 12px Georgia, serif');\n\n      // --- 总阳性率条形图 (D5) ---\n      const barRect = anchorRect('D', 5, W * 0.5, H * 0.12);\n      const barX = barRect.x, barY = barRect.y, barW = barRect.w, barH = barRect.h;\n\n      // 条形图背景\n      ctx.save();\n      ctx.fillStyle = '#f5f0e8';\n      drawRoundedRect(barX, barY, barW, barH, 4);\n      ctx.fill();\n      ctx.strokeStyle = '#d9d2c3';\n      ctx.lineWidth = 1;\n      ctx.stroke();\n      ctx.restore();\n\n      // 总阳性率填充 (最大约12%时填满)\n      const maxRate = 0.12;\n      const barFillW = barW * Math.min(total_positive / maxRate, 1);\n      ctx.save();\n      ctx.fillStyle = 'rgba(192, 57, 43, 0.7)';\n      drawRoundedRect(barX, barY, barFillW, barH, 4);\n      ctx.fill();\n      ctx.restore();\n\n      // 条形图标签\n      label('总阳性率 = ' + (total_positive * 100).toFixed(2) + '%', barX + barW/2, barY - 10, '#c0392b', 'center', 'bold 12px Georgia, serif');\n\n      // 分解标注\n      const truePosW = barW * (p_true_pos / maxRate);\n      const falsePosW = barW * (p_false_pos / maxRate);\n      if (truePosW > 30) {\n        label('真阳性', barX + truePosW/2, barY + barH/2, '#fff', 'center', '11px Georgia, serif');\n      }\n      if (falsePosW > 30) {\n        label('假阳性', barX + truePosW + falsePosW/2, barY + barH/2, '#fff', 'center', '11px Georgia, serif');\n      }\n\n      // --- 连接线 (树状图) ---\n      ctx.save();\n      ctx.strokeStyle = '#b9b3a6';\n      ctx.lineWidth = 1;\n      ctx.setLineDash([4, 3]);\n      // 从患病支到总阳性率\n      ctx.beginPath();\n      ctx.moveTo(sickX + sickW, sickY + sickH/2);\n      ctx.lineTo(barX, barY + barH/2);\n      ctx.stroke();\n      // 从健康支到总阳性率\n      ctx.beginPath();\n      ctx.moveTo(healthyX, healthyY + healthyH/2);\n      ctx.lineTo(barX + barW, barY + barH/2);\n      ctx.stroke();\n      ctx.restore();\n\n      // --- 公式小标签 ---\n      // 已在HTML中\n    }\n\n    // --- 尺寸调整 ------------------------------------------------------\n    function resize() {\n      const r = canvas.getBoundingClientRect();\n      W = r.width;\n      H = r.height;\n      const dpr = window.devicePixelRatio || 1;\n      canvas.width = Math.round(W * dpr);\n      canvas.height = Math.round(H * dpr);\n      ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n      draw();\n    }\n\n    // --- 事件绑定 ------------------------------------------------------\n    const slider = document.getElementById('prevalence-slider');\n    slider.addEventListener('input', function () {\n      prevalence = parseFloat(this.value);\n      draw();\n    });\n\n    // 指针交互 (探针效果: 显示当前患病率位置)\n    canvas.addEventListener('pointermove', function (e) {\n      // 可选: 根据鼠标位置更新患病率? 不, 保持滑块控制\n      // 但可以显示探针虚线, 这里简化, 不实现探针\n    });\n\n    // --- 教师控制钩子 --------------------------------------------------\n    window.hkControl = {\n      set: function (s) {\n        if (s && s.prevalence != null) {\n          prevalence = Math.max(0.001, Math.min(0.1, +s.prevalence));\n          slider.value = prevalence;\n          draw();\n        }\n      },\n      highlight: function (sel) {\n        var el = document.querySelector(sel);\n        if (!el) return;\n        el.style.outline = '2px solid #c0392b';\n        setTimeout(function () { el.style.outline = ''; }, 1500);\n      },\n      annotate: function () {},\n      reveal: function () {}\n    };\n\n    // --- 初始化 --------------------------------------------------------\n    window.addEventListener('resize', resize);\n    resize();\n  </script>\n</body>\n</html>",
        "html_path": null,
        "mermaid": null,
        "layout": "newcol",
        "params": [],
        "controls": []
      },
      "widget_hint": "拖动滑块改变患病率，观察两支贡献的此消彼长。"
    }
  ]
}