{
  "session_id": "sess_5",
  "course_id": "course_9037451b03",
  "title": "因果掩码：别偷看未来",
  "learning_goal": "理解因果掩码如何防止未来信息泄露。",
  "steps": [
    {
      "title": "生成时能偷看下一个词吗？",
      "beat": "hook",
      "spoken_text": "想象你在写句子，写到一半，下一个词还没写出来。模型生成的时候也是这样，它只能看到已经写下的词。板书第一行我写下一个问题：生成第 t 个词的时候，它能偷看第 t+1 个词吗？你先别急着回答，想想如果偷看了会怎样。",
      "boards": [
        {
          "title": "",
          "markdown": "- 生成第 $t$ 个词时，模型能看到谁？\n- 能偷看第 $t+1$ 个词吗？\n- 偷看了会怎样？→ 你先猜",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "能偷看第 $t+1$ 个词吗？",
          "board_index": 0,
          "trigger_phrase": "能偷看第 t+1 个词吗",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "生成第 t 个词的时候，模型能偷看第 t+1 个词吗？",
        "options": [
          "能，反正训练时都看过，看一眼没关系",
          "不能，生成时未来词根本还没出现"
        ],
        "correct_index": 1,
        "misconceptions": [
          "这是常见的混淆：训练时确实见过全部词，但生成是一步一步来的，第 t+1 个词还没被写出来，模型没有东西可看。",
          null
        ],
        "explanation": "生成是自回归的，第 t 步时第 t+1 个词还不存在，模型只能看已经生成的词。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "先 softmax 还是先掩码？",
      "beat": "poe",
      "spoken_text": "现在有个操作问题。我们想让未来词的注意力权重变成零。板书第二行我写了两种做法：一种是在 softmax 之后把未来词权重直接设为零；另一种是在 softmax 之前把未来词的分数设成负无穷。你先猜哪种对？注意看第一行的公式，softmax 的分母是所有词的指数和，动一个分子，分母也会变。",
      "boards": [
        {
          "title": "两种做法",
          "markdown": "- 做法 A：先 softmax → 再把未来词权重置零\n- 做法 B：先把未来词分数设 $-\\infty$ → 再 softmax\n- 关键：softmax 分母 = 所有词指数和\n- 动一个分子，分母也变 → 你先猜",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "highlight",
          "snippet": "softmax 分母 = 所有词指数和",
          "board_index": 0,
          "trigger_phrase": "softmax 的分母是所有词的指数和",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "想让未来词权重为零，应该先 softmax 还是先掩码？",
        "options": [
          "先 softmax，再把未来词权重设成零，反正结果一样",
          "先掩码再 softmax，因为 softmax 的分母会跟着变"
        ],
        "correct_index": 1,
        "misconceptions": [
          "这是典型误区：以为 softmax 之后置零就行。但 softmax 已经用未来词算过分母了，置零后剩下的权重和不再是 1，而且未来词的信息已经混进去了。",
          null
        ],
        "explanation": "softmax 的分母包含所有词的指数和，如果先算再置零，未来词已经影响了分母，剩下的权重和不为 1；先掩码让未来词指数为零，分母自然只含可见词。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "因果掩码的定义",
      "beat": "define",
      "spoken_text": "所以正确做法是：在 softmax 之前，把 QK 转置矩阵的上三角全部填成负无穷。板书第三行我圈出来的就是这个操作。负无穷经过指数变成零，未来词的权重正好是零，分母也只算可见词。这就是因果掩码。看最后一行，掩码矩阵长什么样？上三角全是负无穷，下三角全是零。",
      "boards": [
        {
          "title": "因果掩码",
          "markdown": "- 在 $QK^{\\top}$ 上三角填 $-\\infty$\n- 再 softmax → 未来词权重 = **0**\n- 掩码矩阵：上三角 $-\\infty$，下三角 $0$\n- 名字：**因果掩码**",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "在 $QK^{\\top}$ 上三角填 $-\\infty$",
          "board_index": 0,
          "trigger_phrase": "把 QK 转置矩阵的上三角全部填成负无穷",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "三词句的掩码实例",
      "beat": "worked_example",
      "spoken_text": "拿一个只有三个词的句子来走一遍。左边这张图是三乘三的分数矩阵，上三角那三个格子我填了负无穷，就是词一不能看词二词三、词二不能看词三。右边这张图是 softmax 之后的结果，你看未来位置全是零，下三角还留着非零权重。那问题来了：词一这一行，softmax 之后它的注意力全给了谁？",
      "boards": [
        {
          "title": "三词句走一遍",
          "markdown": "- 3 个词 → $3\\times 3$ 分数矩阵\n- 上三角 3 格填 $-\\infty$（词1不看词2词3）\n- softmax 后：未来位置 = **0**\n- 下三角保留非零权重",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "未来位置 = **0**",
          "board_index": 0,
          "trigger_phrase": "未来位置全是零",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": {
        "mode": "choice",
        "question": "词一这一行，softmax 之后它的注意力全给了谁？",
        "options": [
          "给了词一自己，因为未来两个位置都是零",
          "给了词二，因为词二离得近",
          "平均分给三个词，softmax 会摊平"
        ],
        "correct_index": 0,
        "misconceptions": [
          null,
          "这是位置直觉，但词二在词一的未来里，已经被 -∞ 挡掉了。",
          "softmax 不会把 -∞ 摊成非零，指数后它严格是零。"
        ],
        "explanation": "未来位置被 -∞ 压成零，剩下的非零权重只能落在词一自己身上，所以词一只看自己。"
      },
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "softmax 前掩码 vs 后掩码",
      "beat": "contrast",
      "spoken_text": "回看上一段的板书，我们是在 softmax 之前填的负无穷。现在把两种做法摆在一起看这张表。左边这一列是先 softmax 再置零：分母里还留着未来词的指数，归一化就错了，剩下的权重加起来不到一。右边这一列是先填负无穷再 softmax：指数直接变零，分母干净，权重加起来正好是一。看右边这一列最后一行，这就是我们要的。",
      "boards": [
        {
          "title": "两种做法对比",
          "markdown": "| 步骤 | 先 softmax 后置零 | 先填 $-\\infty$ 再 softmax |\n|---|---|---|\n| 未来词指数 | 保留在分母 | 变成 0 |\n| 归一化 | 权重和 < 1 | 权重和 = 1 |\n| 未来权重 | 手动置 0 | 自动为 **0** |",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "highlight",
          "snippet": "自动为 **0**",
          "board_index": 0,
          "trigger_phrase": "这就是我们要的",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "掩码在生成中的应用",
      "beat": "apply",
      "spoken_text": "最后把掩码放回生成现场。模型每写一个词，都要重新算一次注意力，而每一次都套同一张因果掩码：当前位置只能看它左边。板书最后一行我写了这件事决定了什么——它决定了生成的顺序性，模型没法提前引用还没写的词。所以训练时并行算整句，也不会泄露答案。",
      "boards": [
        {
          "title": "每一步都套掩码",
          "markdown": "- 生成第 $t$ 个词 → 重算注意力\n- 每次都套同一张因果掩码\n- 当前位置只看左边 → **顺序性**\n- 训练并行算整句，也不泄露未来",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "顺序性",
          "board_index": 0,
          "trigger_phrase": "决定了生成的顺序性",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": null,
      "new_page_title": null
    },
    {
      "title": "回顾因果掩码",
      "beat": "recap",
      "spoken_text": "最后把整节课收成三行。第一行，掩码动的是分数，不是权重：在 QK 转置矩阵的上三角填负无穷。第二行，填完再 softmax，未来词的权重正好是零。第三行，每次生成都套同一张掩码，当前位置只看左边，顺序性就守住了。回看开头那个问题——能偷看下一个词吗？现在你手里有答案了。",
      "boards": [
        {
          "title": "因果掩码 · 三句话",
          "markdown": "- 掩码动的是**分数**：$QK^{\\top}$ 上三角填 $-\\infty$\n- 填完再 softmax → 未来词权重 = **0**\n- 每次生成都套同一张掩码 → 当前位置只看左边\n- 能偷看下一个词吗？→ **不能**",
          "layout": "follow"
        }
      ],
      "decorations": [
        {
          "kind": "circle",
          "snippet": "上三角填 $-\\infty$",
          "board_index": 0,
          "trigger_phrase": "上三角填负无穷",
          "color": null
        },
        {
          "kind": "highlight",
          "snippet": "未来词权重 = **0**",
          "board_index": 0,
          "trigger_phrase": "未来词的权重正好是零",
          "color": null
        }
      ],
      "illustration": null,
      "widget": null,
      "question": null,
      "reward": {
        "title": "因果掩码",
        "description": "在 QK 转置矩阵上三角填负无穷再 softmax，未来词权重为零，生成时只能看左边。"
      },
      "new_page_title": null
    }
  ],
  "exercises": [
    {
      "exercise_id": "sess_5_ex1",
      "kind": "fill_blank",
      "stem": "因果掩码在 $QK^{\\top}$ 矩阵的____位置填入 $-\\infty$，使得 softmax 之后未来词的注意力权重为零。",
      "options": [],
      "correct_index": null,
      "answer": "上三角",
      "accepted": [
        "上三角区域",
        "上三角部分",
        "上三角元素",
        "上三角区"
      ],
      "explanation": "因果掩码的核心操作是在分数矩阵的上三角位置填 $-\\infty$，因为上三角对应未来词的位置，填 $-\\infty$ 后 softmax 时这些位置的指数变为零，从而防止看到未来信息。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_5_ex2",
      "kind": "single_choice",
      "stem": "在因果掩码中，为什么要在 softmax 之前把未来词的分数设为 $-\\infty$，而不是在 softmax 之后把未来词的权重置零？",
      "options": [
        "因为 softmax 之后置零会改变分母，导致剩余权重之和小于 1",
        "因为 softmax 之前置 $-\\infty$ 计算更简单，不需要额外操作",
        "因为 softmax 之后置零会使得未来词的分数变为零",
        "因为 softmax 之前置 $-\\infty$ 可以保留未来词的信息"
      ],
      "correct_index": 0,
      "answer": null,
      "accepted": [],
      "explanation": "softmax 的分母是所有词指数之和。如果在 softmax 之后置零，分母中仍包含未来词的指数，归一化后剩余权重之和小于 1；而在 softmax 之前置 $-\\infty$，未来词指数变为 0，分母只含可见词，权重之和正好为 1。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_5_ex3",
      "kind": "single_choice",
      "stem": "假设一个三词句的 $QK^{\\top}$ 分数矩阵（未掩码）为 $\\begin{bmatrix} 1 & 2 & 3 \\\\ 4 & 5 & 6 \\\\ 7 & 8 & 9 \\end{bmatrix}$，经过因果掩码并 softmax 后，第一行（词1）的注意力权重分布最可能是什么？",
      "options": [
        "全部为 0",
        "集中在第一个位置，其余为 0",
        "均匀分布在三个位置",
        "集中在第二个位置"
      ],
      "correct_index": 1,
      "answer": null,
      "accepted": [],
      "explanation": "因果掩码将上三角（未来位置）设为 $-\\infty$，所以词1只能看到自己（位置1）。softmax 后，位置1的权重为 1，位置2和3的权重为 0。因此权重集中在第一个位置。",
      "widget": null,
      "widget_hint": ""
    },
    {
      "exercise_id": "sess_5_ex4",
      "kind": "interactive",
      "stem": "通过调整掩码的边界，观察 softmax 后注意力权重的变化，判断哪个选项正确描述了因果掩码的作用。",
      "options": [
        "因果掩码使未来位置的权重为零，且每行权重之和为 1",
        "因果掩码使未来位置的权重为零，但每行权重之和小于 1",
        "因果掩码使未来位置的权重不为零，但每行权重之和为 1",
        "因果掩码使未来位置的权重不为零，且每行权重之和小于 1"
      ],
      "correct_index": 0,
      "answer": null,
      "accepted": [],
      "explanation": "因果掩码在 softmax 之前将未来位置分数设为 $-\\infty$，softmax 后这些位置权重为 0，且分母只包含可见词，因此每行权重之和为 1。",
      "widget": {
        "kind": "explorable",
        "title": "因果掩码交互演示",
        "task": "拖动滑块调整掩码的上三角边界（即设置未来词分数为 $-\\infty$ 的范围），观察 softmax 后注意力权重矩阵的变化。注意未来位置的权重是否为零，以及每行权重之和是否为 1。",
        "html": "<!DOCTYPE html>\n<html>\n<head><script>/**\n * HandChart — zero-dependency hand-drawn chart base for explorable widgets.\n *\n * Injected into every generated 2D explorable (widget_generator.py) so the\n * model supplies data + overlays instead of hand-writing axes. The look matches\n * the handwritten page: paper panel, slightly wobbly ink axes, main red /\n * aux green, handwriting labels.\n *\n * API (all coordinates in WORLD units unless noted):\n *   const ch = HandChart.render(canvas, opts)\n *     opts = { type: \"function\"|\"points\"|\"bars\"|\"lines\",\n *              xlim:[x0,x1], ylim:[y0,y1],                  (all but bars)\n *              fn: x=>y | fns:[{fn, color?, label?}],        (function)\n *              points:[[x,y],...],                          (points)\n *              series:[{name, points, color?, dash?}],      (lines; legend drawn)\n *              bars:{labels:[...], values:[...], highlight?} (bars)\n *              xlabel, ylabel, grid:true|false, title }\n *     returns { ctx, sx, sy, box, W, H, opts }  — keep it to draw overlays\n *   HandChart.marker(ch, x, y, {color, label, r})       dot + optional label\n *   HandChart.vline(ch, x, {color, dash, label})         vertical guide\n *   HandChart.hline(ch, y, {color, dash, label})         horizontal guide\n *   HandChart.segment(ch, [x1,y1], [x2,y2], {color, width, dash, arrow})\n *   HandChart.note(ch, x, y, text, {color, align, size}) handwriting annotation\n *   HandChart.attachProbe(canvas, {xlim, ylim}, onProbe, onLeave?)\n *     pointer follow; onProbe({x, y}) in world coords — redraw + readouts there.\n *   HandChart.style = { INK, AXIS, MAIN, AUX, GRID, PAPER, MUTED }\n */\n(function () {\n  const style = {\n    INK: \"#2b2b2b\", AXIS: \"#8a8377\", MAIN: \"#c0392b\", AUX: \"#2e8b6f\",\n    GRID: \"#ece5d4\", PAPER: \"#fbfaf6\", MUTED: \"#8a8377\", PURPLE: \"#7c3aed\", BLUE: \"#2563eb\",\n  };\n  const PALETTE = [style.MAIN, style.AUX, style.BLUE, style.PURPLE, \"#d97706\"];\n  const PAD = { l: 48, r: 18, t: 18, b: 36 };\n  const HAND = \"'Patrick Hand', 'LXGW WenKai Lite', 'PingFang SC', sans-serif\";\n\n  function font(size, italic) { return (italic ? \"italic \" : \"\") + size + \"px \" + HAND; }\n\n  // deterministic wobble so the \"hand\" does not shake on every redraw\n  function wob(seed, amp) { return Math.sin(seed * 12.9898) * amp; }\n\n  function tickVals(min, max, n) {\n    const span = max - min || 1;\n    const raw = span / n;\n    const mag = Math.pow(10, Math.floor(Math.log10(raw)));\n    const step = [1, 2, 2.5, 5, 10].map((m) => m * mag).find((s) => span / s <= n) || mag * 10;\n    const out = [];\n    for (let v = Math.ceil(min / step) * step; v <= max + step * 1e-9; v += step) out.push(+v.toFixed(10));\n    return out;\n  }\n\n  function fmt(v) {\n    const a = Math.abs(v);\n    if (a === 0) return \"0\";\n    if (a >= 10000 || a < 0.01) return v.toExponential(1);\n    return String(+v.toFixed(Math.max(0, 3 - Math.floor(Math.log10(a)))));\n  }\n\n  /** a hand-drawn line: two passes with tiny deterministic wobble */\n  function inkLine(ctx, x1, y1, x2, y2, color, width, seed) {\n    ctx.save();\n    ctx.strokeStyle = color; ctx.lineWidth = width; ctx.lineCap = \"round\";\n    const n = 6, s = seed || (x1 + y2);\n    ctx.beginPath();\n    for (let i = 0; i <= n; i++) {\n      const t = i / n, x = x1 + (x2 - x1) * t, y = y1 + (y2 - y1) * t;\n      const w = i === 0 || i === n ? 0 : wob(s + i, 0.7);\n      i ? ctx.lineTo(x + w, y + w) : ctx.moveTo(x, y);\n    }\n    ctx.stroke();\n    ctx.restore();\n  }\n\n  function frame(ctx, W, H) {\n    ctx.clearRect(0, 0, W, H);\n    ctx.save();\n    ctx.fillStyle = style.PAPER;\n    ctx.fillRect(0, 0, W, H);\n    ctx.restore();\n  }\n\n  function plotBox(W, H, opts) {\n    const t = PAD.t + (opts && opts.title ? 18 : 0);\n    return { x0: PAD.l, y0: t, x1: W - PAD.r, y1: H - PAD.b };\n  }\n\n  function mapping(box, xlim, ylim) {\n    const [x0, x1] = xlim, [y0, y1] = ylim;\n    return {\n      sx: (v) => box.x0 + (v - x0) / (x1 - x0) * (box.x1 - box.x0),\n      sy: (v) => box.y1 - (v - y0) / (y1 - y0) * (box.y1 - box.y0),\n    };\n  }\n\n  function axes(ctx, box, opts) {\n    const [x0, x1] = opts.xlim, [y0, y1] = opts.ylim;\n    const { sx, sy } = mapping(box, opts.xlim, opts.ylim);\n    const xt = tickVals(x0, x1, 6).filter((v) => v >= x0 && v <= x1);\n    const yt = tickVals(y0, y1, 4).filter((v) => v >= y0 && v <= y1);\n    if (opts.grid !== false) {\n      ctx.save(); ctx.strokeStyle = style.GRID; ctx.lineWidth = 1;\n      for (const v of xt) { ctx.beginPath(); ctx.moveTo(sx(v), box.y0); ctx.lineTo(sx(v), box.y1); ctx.stroke(); }\n      for (const v of yt) { ctx.beginPath(); ctx.moveTo(box.x0, sy(v)); ctx.lineTo(box.x1, sy(v)); ctx.stroke(); }\n      ctx.restore();\n    }\n    // axes sit on zero when zero is inside the range, else on the box edge\n    const ax = y0 <= 0 && y1 >= 0 ? sy(0) : box.y1;\n    const ay = x0 <= 0 && x1 >= 0 ? sx(0) : box.x0;\n    inkLine(ctx, box.x0 - 4, ax, box.x1 + 6, ax, style.AXIS, 1.4, 1);\n    inkLine(ctx, ay, box.y1 + 4, ay, box.y0 - 6, style.AXIS, 1.4, 2);\n    // arrow heads\n    ctx.save(); ctx.fillStyle = style.AXIS;\n    ctx.beginPath(); ctx.moveTo(box.x1 + 8, ax); ctx.lineTo(box.x1 + 1, ax - 3.5); ctx.lineTo(box.x1 + 1, ax + 3.5); ctx.fill();\n    ctx.beginPath(); ctx.moveTo(ay, box.y0 - 8); ctx.lineTo(ay - 3.5, box.y0 - 1); ctx.lineTo(ay + 3.5, box.y0 - 1); ctx.fill();\n    ctx.restore();\n    ctx.save();\n    ctx.fillStyle = style.MUTED; ctx.font = font(12);\n    ctx.textAlign = \"center\";\n    for (const v of xt) {\n      if (Math.abs(sx(v) - ay) < 1 && ay !== box.x0) continue; // skip the label under the y axis\n      inkLine(ctx, sx(v), ax - 3, sx(v), ax + 3, style.AXIS, 1, v);\n      ctx.fillText(fmt(v), sx(v), ax + 16);\n    }\n    ctx.textAlign = \"right\";\n    for (const v of yt) {\n      if (Math.abs(sy(v) - ax) < 1 && ax !== box.y1) continue;\n      inkLine(ctx, ay - 3, sy(v), ay + 3, sy(v), style.AXIS, 1, v + 7);\n      ctx.fillText(fmt(v), ay - 7, sy(v) + 4);\n    }\n    ctx.font = font(14, true); ctx.fillStyle = style.INK;\n    if (opts.xlabel) { ctx.textAlign = \"right\"; ctx.fillText(opts.xlabel, box.x1 + 4, ax - 8); }\n    if (opts.ylabel) { ctx.textAlign = \"left\"; ctx.fillText(opts.ylabel, ay + 8, box.y0 + 4); }\n    ctx.restore();\n    return { sx, sy };\n  }\n\n  function stroke(ctx, pts, color, width, dash) {\n    if (!pts.length) return;\n    ctx.save();\n    ctx.strokeStyle = color; ctx.lineWidth = width || 2.2;\n    ctx.lineJoin = \"round\"; ctx.lineCap = \"round\";\n    if (dash) ctx.setLineDash(dash);\n    ctx.beginPath();\n    let pen = false;\n    for (const p of pts) {\n      if (!p) { pen = false; continue; }\n      pen ? ctx.lineTo(p[0], p[1]) : ctx.moveTo(p[0], p[1]);\n      pen = true;\n    }\n    ctx.stroke();\n    ctx.restore();\n  }\n\n  function fnPoints(fn, opts, m) {\n    const [x0, x1] = opts.xlim, [y0, y1] = opts.ylim;\n    const n = 400, pts = [];\n    const margin = (y1 - y0) * 0.02;\n    for (let i = 0; i <= n; i++) {\n      const x = x0 + (x1 - x0) * i / n;\n      let y;\n      try { y = fn(x); } catch (e) { y = NaN; }\n      if (!isFinite(y) || y < y0 - margin || y > y1 + margin) { pts.push(null); continue; }\n      pts.push([m.sx(x), m.sy(y)]);\n    }\n    return pts;\n  }\n\n  function legend(ctx, box, items) {\n    if (!items.length) return;\n    ctx.save();\n    ctx.font = font(13); ctx.textAlign = \"left\"; ctx.textBaseline = \"middle\";\n    let x = box.x0 + 10, y = box.y0 + 12;\n    for (const it of items) {\n      inkLine(ctx, x, y, x + 22, y, it.color, 2.4, x);\n      ctx.fillStyle = style.INK;\n      ctx.fillText(it.label, x + 28, y + 1);\n      x += 28 + ctx.measureText(it.label).width + 18;\n    }\n    ctx.restore();\n  }\n\n  function title(ctx, W, text) {\n    ctx.save();\n    ctx.font = font(15); ctx.fillStyle = style.INK; ctx.textAlign = \"center\";\n    ctx.fillText(text, W / 2, 16);\n    ctx.restore();\n  }\n\n  function render(canvas, opts) {\n    const ctx = canvas.getContext(\"2d\");\n    const rect = canvas.getBoundingClientRect();\n    const W = Math.max(60, Math.round(rect.width)), H = Math.max(60, Math.round(rect.height));\n    const dpr = window.devicePixelRatio || 1;\n    if (canvas.width !== Math.round(W * dpr) || canvas.height !== Math.round(H * dpr)) {\n      canvas.width = Math.round(W * dpr); canvas.height = Math.round(H * dpr);\n    }\n    ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n    frame(ctx, W, H);\n    if (opts.title) title(ctx, W, opts.title);\n    const box = plotBox(W, H, opts);\n    const type = opts.type || \"function\";\n\n    if (type === \"bars\") {\n      const labels = opts.bars.labels || [], values = opts.bars.values || [];\n      const n = Math.max(1, values.length);\n      const bw = (box.x1 - box.x0) / n;\n      const vmax = Math.max(...values.map((v) => Math.abs(v)), 0) * 1.15 || 1;\n      const base = box.y1;\n      inkLine(ctx, box.x0 - 4, base, box.x1 + 4, base, style.AXIS, 1.4, 3);\n      ctx.save();\n      values.forEach((v, i) => {\n        const h = (Math.abs(v) / vmax) * (box.y1 - box.y0);\n        const x = box.x0 + i * bw + bw * 0.2, w = bw * 0.6, y = base - h;\n        const hot = opts.bars.highlight != null && i === opts.bars.highlight;\n        ctx.fillStyle = hot ? style.MAIN : \"#e6dfcf\";\n        ctx.fillRect(x, y, w, h);\n        ctx.strokeStyle = hot ? style.MAIN : style.AXIS; ctx.lineWidth = 1.2;\n        ctx.strokeRect(x + 0.5, y + 0.5, w - 1, h - 1);\n        ctx.font = font(13); ctx.textAlign = \"center\"; ctx.fillStyle = style.INK;\n        ctx.fillText(String(labels[i] == null ? i + 1 : labels[i]), x + w / 2, base + 17);\n        ctx.fillStyle = hot ? style.MAIN : style.MUTED;\n        ctx.fillText(fmt(v), x + w / 2, y - 6);\n      });\n      ctx.restore();\n      const sx = (i) => box.x0 + (i + 0.5) * bw, sy = (v) => base - (Math.abs(v) / vmax) * (box.y1 - box.y0);\n      return { ctx, sx, sy, box, W, H, opts };\n    }\n\n    const m = axes(ctx, box, opts);\n    const items = [];\n    if (type === \"lines\") {\n      (opts.series || []).forEach((s, i) => {\n        const color = s.color || PALETTE[i % PALETTE.length];\n        stroke(ctx, (s.points || []).map((p) => [m.sx(p[0]), m.sy(p[1])]), color, 2.2, s.dash);\n        if (s.name) items.push({ label: s.name, color });\n      });\n    } else if (type === \"points\") {\n      ctx.save(); ctx.fillStyle = opts.color || style.MAIN;\n      (opts.points || []).forEach((p) => { ctx.beginPath(); ctx.arc(m.sx(p[0]), m.sy(p[1]), opts.r || 3.6, 0, Math.PI * 2); ctx.fill(); });\n      ctx.restore();\n    } else {\n      const fns = opts.fns || (typeof opts.fn === \"function\" ? [{ fn: opts.fn, color: opts.color, label: opts.label }] : []);\n      fns.forEach((f, i) => {\n        const color = f.color || PALETTE[i % PALETTE.length];\n        stroke(ctx, fnPoints(f.fn, opts, m), color, f.width || 2.4, f.dash);\n        if (f.label) items.push({ label: f.label, color });\n      });\n    }\n    legend(ctx, box, items);\n    return { ctx, sx: m.sx, sy: m.sy, box, W, H, opts };\n  }\n\n  // ---- overlays (world coordinates) ----\n  function marker(ch, x, y, o) {\n    o = o || {};\n    const px = ch.sx(x), py = ch.sy(y);\n    ch.ctx.save();\n    ch.ctx.fillStyle = o.color || style.MAIN;\n    ch.ctx.beginPath(); ch.ctx.arc(px, py, o.r || 5, 0, Math.PI * 2); ch.ctx.fill();\n    ch.ctx.strokeStyle = style.PAPER; ch.ctx.lineWidth = 1.5; ch.ctx.stroke();\n    if (o.label) {\n      ch.ctx.font = font(o.size || 13); ch.ctx.fillStyle = o.color || style.MAIN; ch.ctx.textAlign = \"left\";\n      ch.ctx.fillText(o.label, px + 8, py - 8);\n    }\n    ch.ctx.restore();\n  }\n\n  function vline(ch, x, o) {\n    o = o || {};\n    const px = ch.sx(x);\n    stroke(ch.ctx, [[px, ch.box.y0], [px, ch.box.y1]], o.color || style.MUTED, o.width || 1.2, o.dash || [4, 4]);\n    if (o.label) note(ch, x, null, o.label, { color: o.color || style.MUTED, px: px + 4, py: ch.box.y0 + 12, align: \"left\", size: o.size });\n  }\n\n  function hline(ch, y, o) {\n    o = o || {};\n    const py = ch.sy(y);\n    stroke(ch.ctx, [[ch.box.x0, py], [ch.box.x1, py]], o.color || style.MUTED, o.width || 1.2, o.dash || [4, 4]);\n    if (o.label) note(ch, null, y, o.label, { color: o.color || style.MUTED, px: ch.box.x1 - 4, py: py - 5, align: \"right\", size: o.size });\n  }\n\n  function segment(ch, a, b, o) {\n    o = o || {};\n    const x1 = ch.sx(a[0]), y1 = ch.sy(a[1]), x2 = ch.sx(b[0]), y2 = ch.sy(b[1]);\n    const color = o.color || style.AUX;\n    stroke(ch.ctx, [[x1, y1], [x2, y2]], color, o.width || 2, o.dash);\n    if (o.arrow) {\n      const ang = Math.atan2(y2 - y1, x2 - x1), L = 9;\n      ch.ctx.save(); ch.ctx.fillStyle = color; ch.ctx.beginPath();\n      ch.ctx.moveTo(x2, y2);\n      ch.ctx.lineTo(x2 - L * Math.cos(ang - 0.4), y2 - L * Math.sin(ang - 0.4));\n      ch.ctx.lineTo(x2 - L * Math.cos(ang + 0.4), y2 - L * Math.sin(ang + 0.4));\n      ch.ctx.fill(); ch.ctx.restore();\n    }\n  }\n\n  function note(ch, x, y, text, o) {\n    o = o || {};\n    const px = o.px != null ? o.px : ch.sx(x), py = o.py != null ? o.py : ch.sy(y);\n    ch.ctx.save();\n    ch.ctx.font = font(o.size || 14, o.italic);\n    ch.ctx.fillStyle = o.color || style.INK;\n    ch.ctx.textAlign = o.align || \"left\";\n    ch.ctx.fillText(text, px, py);\n    ch.ctx.restore();\n  }\n\n  /** Pointer probe in world coords; the caller redraws and updates readouts. */\n  function attachProbe(canvas, opts, onProbe, onLeave) {\n    const [x0, x1] = opts.xlim;\n    const [y0, y1] = opts.ylim;\n    const toWorld = (px, py) => {\n      const r = canvas.getBoundingClientRect();\n      const box = plotBox(r.width, r.height, opts);\n      const t = (px - r.left - box.x0) / (box.x1 - box.x0);\n      const u = 1 - (py - r.top - box.y0) / (box.y1 - box.y0);\n      return { x: x0 + t * (x1 - x0), y: y0 + u * (y1 - y0), inside: t >= 0 && t <= 1 };\n    };\n    const handler = (e) => {\n      const p = toWorld(e.clientX, e.clientY);\n      if (p.inside) onProbe(p);\n    };\n    canvas.addEventListener(\"pointermove\", handler);\n    canvas.addEventListener(\"pointerdown\", handler);\n    if (onLeave) canvas.addEventListener(\"pointerleave\", onLeave);\n    canvas.style.touchAction = \"none\";\n    return toWorld;\n  }\n\n  window.HandChart = { render, marker, vline, hline, segment, note, attachProbe, style, font: HAND };\n})();\n</script>\n  <meta charset=\"utf-8\">\n  <style>\n    html, body { margin: 0; height: 100%; background: #faf8f3; font-family: \"PingFang SC\", \"Hiragino Sans GB\", sans-serif; color: #333; overflow: hidden; }\n    #wrap { position: absolute; inset: 0; display: flex; flex-direction: column; }\n    #plot { flex: 1; min-height: 0; margin: 10px 10px 4px; border: 1px solid #d9d2c3; border-radius: 12px; background: #fbfaf6; cursor: crosshair; touch-action: none; }\n    #readout { display: flex; justify-content: center; align-items: center; gap: 28px; padding: 4px 0 8px; font-size: 12px; color: #666; flex-wrap: wrap; }\n    #readout .k { display: block; font-size: 11px; letter-spacing: .04em; }\n    #readout .v { display: block; font-family: ui-monospace, Menlo, monospace; font-size: 14px; font-weight: 700; color: #c0392b; min-width: 64px; text-align: center; }\n    #slider-row { display: flex; justify-content: center; align-items: center; gap: 16px; padding: 2px 0 6px; }\n    #slider-row label { font-size: 12px; color: #666; }\n    #maskBoundary-slider { width: 180px; accent-color: #c0392b; }\n    #formula { display: inline-block; padding: 3px 10px; border: 1px solid #d9d2c3; border-radius: 6px; background: #fff; font-family: Georgia, \"Times New Roman\", serif; font-style: italic; font-size: 13px; color: #333; }\n  </style>\n</head>\n<body>\n  <div id=\"wrap\">\n    <canvas id=\"plot\"></canvas>\n    <div id=\"readout\">\n      <span><span class=\"k\">未来位置权重总和</span><span class=\"v\" id=\"futureWeightSum-display\">—</span></span>\n      <span><span class=\"k\">每行权重和</span><span class=\"v\" id=\"rowSum-display\">—</span></span>\n      <span id=\"formula\">causal mask: softmax(QKᵀ/√d) ⊙ M</span>\n    </div>\n    <div id=\"slider-row\">\n      <label for=\"maskBoundary-slider\">掩码边界</label>\n      <input type=\"range\" id=\"maskBoundary-slider\" min=\"0\" max=\"5\" step=\"1\" value=\"5\">\n      <span style=\"font-family:ui-monospace,Menlo,monospace;font-size:13px;color:#c0392b;\" id=\"maskBoundary-value\">5</span>\n    </div>\n  </div>\n  <script>\n    (function() {\n      // ---------- 数学：因果掩码注意力矩阵 ----------\n      // 6x6 矩阵，行 i，列 j，i 为查询位置，j 为键位置\n      // 掩码规则：如果 j > i + (5 - maskBoundary) ？ 实际上我们用 maskBoundary 从 0 到 5：\n      // maskBoundary = 0: 无掩码（所有 j 都可见）\n      // maskBoundary = 5: 完全因果掩码（只允许 j <= i），即上三角全掩码\n      // 一般：允许 j <= i + (5 - maskBoundary)？ 不对，应该是允许 j <= i + (maskBoundary - 5) ？我们重新定义：\n      // 需求：maskBoundary 从 0 到 5，0 表示无掩码，5 表示完全掩码上三角。\n      // 更自然的：允许的列 j 满足 j <= i + (maskBoundary - 5)？当 maskBoundary=5 时 j<=i，当 maskBoundary=0 时 j<=i-5（即全部可见？不对）\n      // 重新考虑：用 maskBoundary 表示“从对角线向上掩码多少条对角线”。\n      // 定义：允许 j <= i + (5 - maskBoundary) ？当 maskBoundary=5 -> j<=i（因果），当 maskBoundary=0 -> j<=i+5（全部可见）。\n      // 但 maskBoundary=0 时 j<=i+5 对于 i=0..5 就是全部列，正确。maskBoundary=5 时 j<=i，正确。\n      // 所以公式：允许的 j <= i + (5 - maskBoundary)。\n      // 当 maskBoundary=0，i+5>=5，全部可见；maskBoundary=5，i+0=i，因果。\n      // 掩码位置：j > i + (5 - maskBoundary) 的置为 -Inf，softmax 后为 0。\n\n      var N = 6;\n      var rawScores = []; // 固定随机分数，保证每次刷新一致\n      (function initScores() {\n        var seed = 42;\n        function rnd() { seed = (seed * 9301 + 49297) % 233280; return seed / 233280; }\n        for (var i = 0; i < N; i++) {\n          rawScores[i] = [];\n          for (var j = 0; j < N; j++) {\n            rawScores[i][j] = (rnd() - 0.5) * 2.5; // -1.25..1.25\n          }\n        }\n      })();\n\n      function computeAttention(maskBoundary) {\n        var attn = [];\n        for (var i = 0; i < N; i++) {\n          attn[i] = [];\n          var maxScore = -Infinity;\n          // 先找未掩码的最大值\n          for (var j = 0; j < N; j++) {\n            var allowed = (j <= i + (5 - maskBoundary));\n            if (allowed) {\n              if (rawScores[i][j] > maxScore) maxScore = rawScores[i][j];\n            }\n          }\n          // 计算 exp 和\n          var exps = [];\n          var sumExp = 0;\n          for (var j = 0; j < N; j++) {\n            var allowed = (j <= i + (5 - maskBoundary));\n            var e = allowed ? Math.exp(rawScores[i][j] - maxScore) : 0;\n            exps[j] = e;\n            sumExp += e;\n          }\n          // 归一化\n          for (var j = 0; j < N; j++) {\n            attn[i][j] = exps[j] / sumExp;\n          }\n        }\n        return attn;\n      }\n\n      // ---------- 绘图设置 ----------\n      var canvas = document.getElementById('plot');\n      var ctx = canvas.getContext('2d');\n      var W = 0, H = 0;\n      var PAD = { l: 48, r: 48, t: 36, b: 36 };\n      var maskBoundary = 5; // 默认完全掩码\n      var attnMatrix = null;\n\n      // 热力图区域：左边 60% 宽度用于矩阵，右边 40% 用于条形图\n      function getLayout() {\n        var plotW = W - PAD.l - PAD.r;\n        var plotH = H - PAD.t - PAD.b;\n        var matrixW = plotW * 0.55;\n        var matrixH = plotH * 0.9;\n        var matrixX = PAD.l;\n        var matrixY = PAD.t + (plotH - matrixH) * 0.5;\n        var cellW = matrixW / N;\n        var cellH = matrixH / N;\n        var barX = PAD.l + matrixW + 24;\n        var barY = matrixY;\n        var barW = plotW - matrixW - 24;\n        var barH = matrixH;\n        return { matrixX: matrixX, matrixY: matrixY, cellW: cellW, cellH: cellH, matrixW: matrixW, matrixH: matrixH,\n                 barX: barX, barY: barY, barW: barW, barH: barH };\n      }\n\n      function resize() {\n        var r = canvas.getBoundingClientRect();\n        W = r.width; H = r.height;\n        var dpr = window.devicePixelRatio || 1;\n        canvas.width = Math.round(W * dpr);\n        canvas.height = Math.round(H * dpr);\n        ctx.setTransform(dpr, 0, 0, dpr, 0, 0);\n        draw();\n      }\n\n      // ---------- 绘制函数 ----------\n      function draw() {\n        ctx.clearRect(0, 0, W, H);\n        var lay = getLayout();\n        attnMatrix = computeAttention(maskBoundary);\n\n        // 1. 绘制矩阵热力图\n        var maxVal = 1.0;\n        for (var i = 0; i < N; i++) {\n          for (var j = 0; j < N; j++) {\n            var val = attnMatrix[i][j];\n            var x = lay.matrixX + j * lay.cellW;\n            var y = lay.matrixY + i * lay.cellH;\n            // 颜色：权重越大越红（主色 #c0392b），权重为0显示深灰\n            if (val < 1e-9) {\n              ctx.fillStyle = '#3a3a3a'; // 深色表示掩码\n            } else {\n              // 从浅黄到红\n              var t = Math.min(1, val / maxVal);\n              var r = Math.round(250 - t * 60);\n              var g = Math.round(245 - t * 180);\n              var b = Math.round(230 - t * 190);\n              ctx.fillStyle = 'rgb(' + r + ',' + g + ',' + b + ')';\n            }\n            ctx.fillRect(x, y, lay.cellW - 1, lay.cellH - 1);\n            // 单元格边框\n            ctx.strokeStyle = '#d9d2c3';\n            ctx.lineWidth = 0.5;\n            ctx.strokeRect(x + 0.5, y + 0.5, lay.cellW - 1, lay.cellH - 1);\n            // 数值文本（只对非零显示，避免太挤）\n            if (val > 0.01) {\n              ctx.save();\n              ctx.font = '10px ui-monospace, Menlo, monospace';\n              ctx.fillStyle = val > 0.5 ? '#fff' : '#333';\n              ctx.textAlign = 'center';\n              ctx.textBaseline = 'middle';\n              ctx.fillText(val.toFixed(2), x + lay.cellW / 2, y + lay.cellH / 2);\n              ctx.restore();\n            }\n          }\n        }\n\n        // 2. 矩阵的行列标签\n        ctx.save();\n        ctx.font = 'italic 11px Georgia, serif';\n        ctx.fillStyle = '#8a8377';\n        ctx.textAlign = 'center';\n        ctx.textBaseline = 'middle';\n        for (var j = 0; j < N; j++) {\n          ctx.fillText('k' + (j + 1), lay.matrixX + j * lay.cellW + lay.cellW / 2, lay.matrixY - 10);\n        }\n        ctx.textAlign = 'right';\n        for (var i = 0; i < N; i++) {\n          ctx.fillText('q' + (i + 1), lay.matrixX - 8, lay.matrixY + i * lay.cellH + lay.cellH / 2);\n        }\n        ctx.restore();\n\n        // 3. 掩码边界虚线（沿对角线方向）\n        // 边界：允许 j <= i + (5 - maskBoundary)，即 j - i <= 5 - maskBoundary\n        // 边界线：j - i = 5 - maskBoundary + 0.5 ？ 为了视觉上沿单元格边缘，我们画在 j = i + (5 - maskBoundary) + 0.5 处\n        var boundaryOffset = (5 - maskBoundary) + 0.5; // 单元格索引\n        ctx.save();\n        ctx.strokeStyle = '#c0392b';\n        ctx.lineWidth = 1.8;\n        ctx.setLineDash([6, 4]);\n        ctx.beginPath();\n        // 从矩阵左上角开始，沿对角线方向画线\n        // 边界线方程：j - i = boundaryOffset\n        // 在矩阵区域内画线段\n        var x1, y1, x2, y2;\n        // 计算与矩阵边界的交点\n        var iMin = 0, iMax = N - 1, jMin = 0, jMax = N - 1;\n        // 参数方程：i = t, j = t + boundaryOffset\n        // 找 t 的范围使得 0<=i<=N-1, 0<=j<=N-1\n        var tStart = Math.max(0, -boundaryOffset);\n        var tEnd = Math.min(N - 1, N - 1 - boundaryOffset);\n        if (tStart <= tEnd) {\n          x1 = lay.matrixX + (tStart + boundaryOffset) * lay.cellW;\n          y1 = lay.matrixY + tStart * lay.cellH;\n          x2 = lay.matrixX + (tEnd + boundaryOffset) * lay.cellW;\n          y2 = lay.matrixY + tEnd * lay.cellH;\n          ctx.beginPath();\n          ctx.moveTo(x1, y1);\n          ctx.lineTo(x2, y2);\n          ctx.stroke();\n        }\n        ctx.restore();\n\n        // 4. 右侧条形图：每行权重和\n        var rowSums = [];\n        for (var i = 0; i < N; i++) {\n          var s = 0;\n          for (var j = 0; j < N; j++) s += attnMatrix[i][j];\n          rowSums.push(s);\n        }\n        var barMax = 1.2; // 略大于1\n        var barTotalH = lay.barH;\n        var barGap = 6;\n        var barItemH = (barTotalH - barGap * (N - 1)) / N;\n        ctx.save();\n        ctx.font = 'italic 11px Georgia, serif';\n        ctx.fillStyle = '#8a8377';\n        ctx.textAlign = 'left';\n        ctx.textBaseline = 'middle';\n        ctx.fillText('每行权重和', lay.barX, lay.barY - 12);\n        for (var i = 0; i < N; i++) {\n          var y = lay.barY + i * (barItemH + barGap);\n          // 背景条\n          ctx.fillStyle = '#e8e2d6';\n          ctx.fillRect(lay.barX, y, lay.barW, barItemH);\n          // 实际条\n          var w = (rowSums[i] / barMax) * lay.barW;\n          ctx.fillStyle = '#c0392b';\n          ctx.fillRect(lay.barX, y, w, barItemH);\n          // 标签\n          ctx.fillStyle = '#333';\n          ctx.font = '10px ui-monospace, Menlo, monospace';\n          ctx.textAlign = 'left';\n          ctx.fillText('q' + (i + 1), lay.barX - 28, y + barItemH / 2);\n          ctx.textAlign = 'right';\n          ctx.fillText(rowSums[i].toFixed(3), lay.barX + lay.barW + 4, y + barItemH / 2);\n        }\n        // 参考线 1.0\n        ctx.strokeStyle = '#2e8b6f';\n        ctx.lineWidth = 1;\n        ctx.setLineDash([4, 4]);\n        ctx.beginPath();\n        var refX = lay.barX + (1.0 / barMax) * lay.barW;\n        ctx.moveTo(refX, lay.barY - 4);\n        ctx.lineTo(refX, lay.barY + barTotalH + 4);\n        ctx.stroke();\n        ctx.setLineDash([]);\n        ctx.fillStyle = '#2e8b6f';\n        ctx.font = 'italic 10px Georgia, serif';\n        ctx.textAlign = 'center';\n        ctx.fillText('1.0', refX, lay.barY + barTotalH + 16);\n        ctx.restore();\n\n        // 5. 更新读数\n        var futureSum = 0;\n        for (var i = 0; i < N; i++) {\n          for (var j = i + 1; j < N; j++) {\n            futureSum += attnMatrix[i][j];\n          }\n        }\n        document.getElementById('futureWeightSum-display').textContent = futureSum.toFixed(4);\n        // 每行权重和范围\n        var minSum = Infinity, maxSum = -Infinity;\n        for (var i = 0; i < N; i++) {\n          if (rowSums[i] < minSum) minSum = rowSums[i];\n          if (rowSums[i] > maxSum) maxSum = rowSums[i];\n        }\n        document.getElementById('rowSum-display').textContent = minSum.toFixed(3) + ' ~ ' + maxSum.toFixed(3);\n        document.getElementById('maskBoundary-value').textContent = maskBoundary;\n      }\n\n      // ---------- 交互 ----------\n      function onSliderChange() {\n        var val = parseInt(document.getElementById('maskBoundary-slider').value, 10);\n        maskBoundary = val;\n        draw();\n      }\n\n      document.getElementById('maskBoundary-slider').addEventListener('input', onSliderChange);\n\n      // ---------- 控制钩子 ----------\n      window.hkControl = {\n        set: function(obj) {\n          if (obj && obj.maskBoundary != null) {\n            maskBoundary = Math.max(0, Math.min(5, Math.round(+obj.maskBoundary)));\n            document.getElementById('maskBoundary-slider').value = maskBoundary;\n            draw();\n          }\n        },\n        highlight: function(sel) {\n          var el = document.querySelector(sel);\n          if (!el) return;\n          el.style.outline = '2px solid #c0392b';\n          setTimeout(function() { el.style.outline = ''; }, 1500);\n        },\n        annotate: function() {},\n        reveal: function() {}\n      };\n\n      // ---------- 启动 ----------\n      window.addEventListener('resize', resize);\n      resize();\n    })();\n  </script>\n</body>\n</html>",
        "html_path": null,
        "mermaid": null,
        "layout": "newcol",
        "params": [],
        "controls": []
      },
      "widget_hint": "拖动滑块改变掩码范围，观察权重矩阵中未来位置是否变为零。"
    }
  ]
}